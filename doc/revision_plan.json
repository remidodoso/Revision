{
 "meta": {
  "project": "Revision",
  "updated": "2026-07-21",
  "docs": [
   "revision.md",
   "revision_requirements_v1.md",
   "revision_poc.md",
   "revision_getstarted.md",
   "revision_coding_standard.md"
  ]
 },
 "now": [
  {
   "ts": "2026-07-21",
   "text": "Stage 2 complete and the pane exists: MHALL plays the Harpington Padlington on hardware, and rev-ui-kit can scroll and zoom a region the application paints. Next: ui-06, MHALL visible on the piano roll."
  }
 ],
 "tracks": [
  "bootstrap",
  "core_store",
  "engine",
  "midi",
  "ui",
  "recording",
  "dsp",
  "misc"
 ],
 "items": [
  {
   "id": "boot-01",
   "title": "Create new repository",
   "kind": "major",
   "track": "bootstrap",
   "status": "complete",
   "depends_on": [],
   "requirements": [],
   "links": [
    {
     "doc": "revision_getstarted.md",
     "anchor": "Repo note"
    }
   ],
   "plan": "User creates the new Revision repo, public on GitHub; the revision_* documents migrate there. User-only commits, always. Includes: license decision (checkpoint - before first push; interacts with R-1203 VST3 SDK licensing), .nojekyll for Pages, enable Pages so revision_plan.html becomes the live public dashboard.",
   "verify": "Verified 2026-07-19: repo public at github.com/remidodoso/Revision, docs migrated; Pages live - revision_plan.html and .json serve 200, .md files serve verbatim (text/markdown; no front matter, so Jekyll passes them through even pre-.nojekyll). License decided; LICENSE files created. Completed 2026-07-19 with the 'initial framework' push: .nojekyll deployed (200 on Pages), LICENSE files and Cargo.lock in repo, CI first run green (all four jobs: lint, test, cross, license - the mac-cross-from-linux and cargo-deny assumptions both proven).",
   "notes": [
    {
     "ts": "2026-07-19",
     "text": "License checkpoint resolved: dual MIT OR Apache-2.0. Companion standards recorded in revision_coding_standard.md (Dependencies & licensing): GPL-free dependency tree, minimal-dependency posture, non-redistributable SDKs never vendored. Scaffold to include LICENSE-MIT + LICENSE-APACHE and per-crate license fields (boot-02)."
    },
    {
     "ts": "2026-07-19",
     "text": "Repo created and pushed: github.com/remidodoso/Revision, docs migrated, .nojekyll in place. Remaining: LICENSE files (arrive with boot-02 scaffold), confirm Pages enabled and dashboard serving."
    }
   ],
   "created": "2026-07-19",
   "completed": "2026-07-19"
  },
  {
   "id": "boot-02",
   "title": "[checkpoint] Workspace layout, toolchain, CI skeleton",
   "kind": "major",
   "track": "bootstrap",
   "status": "complete",
   "depends_on": [
    "boot-01"
   ],
   "requirements": [
    "R-101",
    "R-104"
   ],
   "links": [
    {
     "doc": "revision_poc.md",
     "anchor": "Architecture v0"
    },
    {
     "doc": "completed/revision_boot02_proposal.md",
     "anchor": ""
    }
   ],
   "plan": "Propose crate names/directories per the revision_poc crate map, Rust toolchain/edition pins, CI running fmt/clippy/test plus Mac/Linux cross-compile checks. Human approval before creation.",
   "verify": "Proposal (revision_boot02_proposal.md) approved by user with all seven recommendations. Scaffold created; local verification on pinned 1.95.0: cargo check --workspace green (all seven crates), cargo fmt --check clean, cargo clippy --all-targets -D warnings clean, cargo check -p rev-core --target wasm32-unknown-unknown green, file-map JSON parses. CI workflow itself unexercised until first push (user commit) - watch its first run.",
   "notes": [
    {
     "ts": "2026-07-19",
     "text": "Pre-settled in discussion (recorded in revision_coding_standard.md): root virtual workspace manifest, members under src/rust/*, rev- package prefix with unprefixed directories, per-crate inner src/ kept, sibling test modules. Checkpoint proposal to incorporate; toolchain pin and CI skeleton remain open."
    },
    {
     "ts": "2026-07-19",
     "text": "Written proposal complete: revision_boot02_proposal.md (scaffold incl. all seven crates empty, toolchain pin 1.95.0/edition 2024, four-job CI, cargo-deny license audit, file map scaffold, build profiles). Seven decisions requested in its §8; awaiting approval."
    },
    {
     "ts": "2026-07-19",
     "text": "All seven recommendations approved; scaffold implemented and verified locally (see verify). Files: root Cargo.toml, rust-toolchain.toml, deny.toml, .gitignore, LICENSE-MIT, LICENSE-APACHE, README.md, .github/workflows/ci.yml, seven crates under src/rust/, doc/revision_file_map.{json,html}, testdata/.gitkeep. Awaiting user commit; CI first run will complete verification."
    }
   ],
   "created": "2026-07-19",
   "completed": "2026-07-19"
  },
  {
   "id": "boot-03",
   "title": "[checkpoint] Testing stack selection",
   "kind": "major",
   "track": "bootstrap",
   "status": "complete",
   "depends_on": [
    "boot-01"
   ],
   "requirements": [
    "R-204",
    "R-1503"
   ],
   "links": [
    {
     "doc": "revision_getstarted.md",
     "anchor": "Testing doctrine"
    },
    {
     "doc": "completed/revision_boot03_proposal.md",
     "anchor": ""
    }
   ],
   "plan": "Propose unit/property-test framework choices, golden-master harness design, and TMON kill-test scripting approach. Human approval before adoption.",
   "verify": "Proposal (revision_boot03_proposal.md) approved by user, all eight decisions. On-approval scope implemented: rev-testkit + rev-xtask scaffolded (doc-commented), .cargo/config.toml xtask alias working (bare invocation prints usage, exits 1 as designed), coding-standard Tests section updated, file map updated same-gesture (17 entries, all <=140 chars, JSON valid). cargo check/fmt/clippy green on the 9-crate workspace. proptest/criterion deliberately not yet in manifests - they enter with their first consumer (stage 1/2).",
   "notes": [
    {
     "ts": "2026-07-19",
     "text": "Scope grown by discussion: also propose bench framework (criterion vs divan), soak/deadline harness placement (xtask-adjacent), and the perf ledger format. Doctrine settled (see getstarted Testing doctrine): perf tests track, never gate, unless a requirement documents acceptance status and the user singles it out; historical record with build-settings/platform/toolchain/commit metadata from day 1 of RT work, rendered as day-over-day graphs (JSON + HTML viewer pattern, Pages-servable)."
    },
    {
     "ts": "2026-07-19",
     "text": "Written proposal complete: revision_boot03_proposal.md (std harness, proptest, custom golden-master harness with .f32+JSON testdata format, rev-testkit and rev-xtask crates, seeded TMON kill-loop, criterion, perf/ledger.jsonl machine-keyed format). Eight decisions requested in its §8; awaiting approval."
    },
    {
     "ts": "2026-07-19",
     "text": "Amended after file-map-purpose discussion (primary purpose: agent file discovery without excess context): filemap checker is two-way, mismatches are failures. Companion changes landed: CLAUDE.md created (map-first instruction + ground rules), coding standard Bookkeeping section (same-gesture map updates, retrieval-style descs <=140 chars), file map descs rewritten retrieval-style."
    },
    {
     "ts": "2026-07-19",
     "text": "Approved (all eight) and implemented per on-approval scope. Forward flag: implementing xtask filemap needs a JSON-reading approach - serde_json is the natural candidate and will be raised as a dependency checkpoint at that point (also wanted later by xtask perf for ledger records and criterion harvest)."
    }
   ],
   "created": "2026-07-19",
   "completed": "2026-07-19"
  },
  {
   "id": "core-01",
   "title": "[checkpoint] Schema DDL, command vocabulary, journal format",
   "kind": "major",
   "track": "core_store",
   "status": "complete",
   "depends_on": [
    "boot-02",
    "boot-03"
   ],
   "requirements": [
    "R-201",
    "R-205",
    "R-401",
    "R-405",
    "R-407"
   ],
   "links": [
    {
     "doc": "revision_poc.md",
     "anchor": "Core schema sketch v0"
    },
    {
     "doc": "completed/revision_core01_proposal.md",
     "anchor": ""
    }
   ],
   "plan": "Concrete DDL from the schema sketch (snake_case, singular table names), command structs, journal redo/undo payload format. Human approval before implementation.",
   "verify": "Checkpoint satisfied through iterative review (revision_core01_proposal.md): all eight decisions ruled by user (4 amended to 16-bit velocity), phrase pass ruled (six rulings incl. window semantics + offset_tick), tuning/scale redesign grounded in Notorolla consumer inventory, terminology settled (note number / pitch class / degree / midi note; transpose by degree vs chromatically) and applied to revision_requirements_v1.md (21 edits: R-002..R-810 incl. R-509 applicability/idiomatic split, R-401 window, R-402 16-bit velocity, R-405 offset+chromatic, R-423 two transpositions, section-6 Definitions block); schema column renamed note_per_period to match. DDL, command vocabulary, journal format approved; core-02 implementation unblocked.",
   "notes": [
    {
     "ts": "2026-07-19",
     "text": "Convention corrected: table and variable names singular, never plural (getstarted Conventions updated; poc schema sketch renamed accordingly)."
    },
    {
     "ts": "2026-07-19",
     "text": "Written proposal complete: revision_core01_proposal.md - full DDL (meta, tuning, scale, phrase, track, event, instance, tempo_point, journal, snapshot; XOR containers; _tick columns; usec_per_quarter tempo), realized view v0 SQL, command vocabulary (singular names), append-only journal with undo/redo-as-entries, builtin tunings seeded (12-ET/16-ET/JI), deps rusqlite + serde/serde_json. Eight decisions in its §7; awaiting approval."
    },
    {
     "ts": "2026-07-19",
     "text": "Tuning/scale sections substantially revised after design discussion grounded in a read-only Notorolla consumer inventory (grid/playback/transforms/generators/harmony): three-level stack (tuning definition + exact tuning_note rows -> materialized_tuning_instance binder -> persisted materialized_tuning result rows), modulus-keyed scale (aperiodic branch tuning-bound via XOR), signed note numbers with anchor-60 convention, genesis gesture, latest-instance-wins resolution, euclidean-mod + owned-pow laws. Renames queued for the phrase pass: event.degree -> note_number, instance -> phrase_instance, phrase scale/root binding. Terminology (note number / degree / midi note) implies requirements edits (R-002/R-423/R-508/R-509) - user-owned, list to be drafted."
    },
    {
     "ts": "2026-07-19",
     "text": "Convention batch applied: event.note_number rename; instance -> phrase_instance (table, view alias, CreatePhraseInstance/SetPhraseInstanceParam); realized -> v_realized; indexes renamed to [object]_[column...]_i; explicit CROSS JOIN for the correlated generate_series. Coding standard gained SQL section (ANSI joins, identifier conventions, v_/_i, no bare instance) and Arithmetic section (rem_euclid law). Only phrase scale binding remains queued for the phrase pass."
    },
    {
     "ts": "2026-07-19",
     "text": "All eight decisions ruled by user: 1-3, 5-8 as recommended; 4 amended to velocity 0..65535 (MIDI 2.0 16-bit, spec-defined 7-bit boundary translation, UI presents 0..127). Decision 8 approval unblocks serde_json, hence xtask filemap implementation. Remaining before core-01 completes: phrase pass, requirements edit list."
    },
    {
     "ts": "2026-07-19",
     "text": "Phrase pass ruled and folded: (1) scale_id + root columns on phrase; (2) window semantics - length_tick gates onsets and sets loop stride, dormant material preserved, tails ring past the edge, length fixed once set; offset_tick added to phrase_instance (window start into material, Vision play-bars-8-16-of-32); (3) make/unmake = composite gestures, no dedicated commands; (4) meter/instrument binding stay in extra; (5) R-415 v0 fallbacks stated non-precedent; (6) out-of-domain notes DROPPED at compile (user: less musical to invent a weird note than to miss an unrealizable one). v_realized updated with window predicates. Only the requirements edit list remains before core-01 closes."
    },
    {
     "ts": "2026-07-19",
     "text": "Requirements edit list applied (user: apply, make it so) - datum renamed note number throughout; pitch class restored as the periodic mod term; degree reclaimed as the scale term (user correction: degree is a scale term, not a tuning term); midi note = wire value; R-509 rewritten (applicability by notes-per-period, idiomatic fit advisory); optionals in (R-401 window, R-402 16-bit velocity, R-405 offset + chromatic transpose); R-423 two first-class transpositions; MIDI/capture/plugin sections aligned (R-606/607/608/703/714/810). Proposal: note_per_period rename + terminology block. core-01 complete."
    }
   ],
   "created": "2026-07-19",
   "completed": "2026-07-19"
  },
  {
   "id": "core-02",
   "title": "Model types + store (open/create/journal/replay)",
   "kind": "major",
   "track": "core_store",
   "status": "complete",
   "depends_on": [
    "core-01"
   ],
   "requirements": [
    "R-201",
    "R-202",
    "R-205"
   ],
   "links": [
    {
     "doc": "completed/revision_core02_proposal.md",
     "anchor": ""
    }
   ],
   "plan": "Implement core model types and the store crate: project open/create, journaled commands in per-gesture transactions, snapshot/replay.",
   "verify": "Proposal approved (decision 7 amended to a localized query catalog). Implemented: rev-core (id/tick/note/tuning+equal/scale/phrase/command/error, owned exp2+log2 for R-501) and rev-store (schema+v_realized, exec with inverses, journal, undo/redo, replay, genesis, query catalog, project with read-only reader). 75 tests green: 39 core unit + 36 store integration incl. 2 proptest properties (replay reproduces model state; undo is exact). Gates green locally: cargo test, fmt --check, clippy --all-targets -D warnings, wasm32 check on rev-core, Windows native. Property tests found two real bugs, both fixed and documented in the proposal's deviations: SQLite row-id reuse breaking redo (now AUTOINCREMENT) and patches unable to restore NULL (now Change<T>). Verified the flagged open question: temp tables DO work on a read-only connection, so §6f-bis materialization waypoints are viable. CI cross-mac moved to a macOS runner (bundled SQLite compiles C); that job is unexercised until first push.",
   "notes": [
    {
     "ts": "2026-07-20",
     "text": "Proposal written: revision_core02_proposal.md (module layout for rev-core/rev-store, public API shapes, uniform Option<Id> command resolution, gesture-as-closure, genesis content, test plan incl. first proptest strategies, MHALL fixture). Eight decisions in its §8; one new dependency requested (thiserror). Scope split recorded: v_realized created here but tested in core-03; JSON round-trip + TMON in core-04; snapshot dormant."
    },
    {
     "ts": "2026-07-20",
     "text": "Implemented and complete. New dependencies landed (all previously approved): serde, serde_json, thiserror, rusqlite (bundled/series/hooks/backup), proptest (dev). Note for eng-06: rusqlite's generate_series is a per-connection virtual-table module, registered in project::configure and open_reader - any new connection touching v_realized needs it. MHALL fixture and the 16-ET party trick both work end to end at the model layer."
    }
   ],
   "created": "2026-07-19",
   "completed": "2026-07-20"
  },
  {
   "id": "core-03",
   "title": "Realization view + fixture tests",
   "kind": "major",
   "track": "core_store",
   "status": "complete",
   "depends_on": [
    "core-02"
   ],
   "requirements": [
    "R-404",
    "R-405",
    "R-422"
   ],
   "links": [
    {
     "doc": "revision.md",
     "anchor": "6f-bis"
    }
   ],
   "plan": "The v_realized view (direct events UNION instanced events with loop/transpose/clip) with hand-computed fixture tests; the view is the model's executable specification.",
   "verify": "Closed scoped to FLAT realization. The v_realized view and its fixture tests stand (8 tests: window gating, window start, looping, instance-length clipping, chromatic transpose and mute, shared-material editing, MHALL order, the 16-ET party trick). The nesting gap is documented, reproduced by an ignored test, and left for its own checkpoint - R-407/R-422 are NOT satisfied. eng-06 needs nothing from the fix.",
   "notes": [
    {
     "ts": "2026-07-21",
     "text": "Started as the gate on everything toward MHALL: eng-06's compiler reads v_realized, and ui-06's roll reads the same rows. Nothing downstream can be trusted until this is fixture-tested."
    },
    {
     "ts": "2026-07-21",
     "text": "FINDING, blocking honest completion: v_realized does NOT realize nested instances. Its second arm joins phrase_instance to event on e.phrase_id = i.phrase_id - one level deep, no recursion - so a phrase whose content is reached only through a nested instance contributes nothing. Verified: an arrangement instancing a phrase that instances a phrase of two notes realizes ZERO events, not two. R-407 permits nesting and R-422 requires a structured instance to realize as though baked, so this is a requirements gap, not a missing feature. Recorded as an #[ignore]d test (realize.rs: a_nested_instance_realizes_as_though_baked) rather than prose. The fix is a recursive CTE in a checkpointed view definition, plus a depth bound and a cycle guard (R-407 prohibits cycles), which needs approval before implementation. MHALL itself does not nest, so the path to first musical sound is not blocked by this."
    }
   ],
   "created": "2026-07-19",
   "completed": "2026-07-21"
  },
  {
   "id": "core-04",
   "title": "JSON round-trip + TMON v0",
   "kind": "major",
   "track": "core_store",
   "status": "planned",
   "depends_on": [
    "core-02"
   ],
   "requirements": [
    "R-203",
    "R-204",
    "R-1504"
   ],
   "links": [],
   "plan": "Lossless SQLite-to-JSON round-trip with automated test; kill-and-reopen test proving no committed gesture is lost. Stage 1 exit.",
   "verify": "",
   "notes": [],
   "created": "2026-07-19",
   "completed": null
  },
  {
   "id": "eng-01",
   "title": "[checkpoint] Engine-app interface (rings, schedule, telemetry)",
   "kind": "major",
   "track": "engine",
   "status": "complete",
   "depends_on": [
    "boot-02"
   ],
   "requirements": [
    "R-302",
    "R-304",
    "R-1501"
   ],
   "links": [
    {
     "doc": "revision_poc.md",
     "anchor": "Timing engine sketch"
    },
    {
     "doc": "completed/revision_eng01_proposal.md",
     "anchor": "Decisions requested"
    }
   ],
   "plan": "Command-ring message set, schedule chunk format, telemetry format. The most consequential seam; human approval before implementation.",
   "verify": "Proposal approved 2026-07-20, all 21 decisions as recommended, no carve-outs. Requirements R-310 (compensate only system-introduced delay), R-311 (10 ms live-path budget), R-312 (below the engine boundary there is only physics) and R-624 (external instrument pairing) written into revision_requirements_v1.md. Proposal archived to doc/completed/revision_eng01_proposal.md. Settled: four channels/three mechanisms; driver-agnostic Engine::process; callback-is-the-clock; command set v0; schedule envelope with payload deferred to eng-06; rev-log; allocation guard; device and stream policy; rev-engine depends on cpal+rtrb only.",
   "notes": [
    {
     "ts": "2026-07-20",
     "text": "Proposal drafted 2026-07-20: doc/revision_eng01_proposal.md. Awaiting approval. Settles four channels/three mechanisms (command ring, position seqlock, observation ring, return ring), a driver-agnostic Engine::process with cpal and offline drivers, the sample clock and correlation, command set v0, the schedule chunk ENVELOPE (payload deferred to eng-06 because core-03 owes v_realized), the observation path plus a new rev-log crate, the allocation guard, and two dependencies (cpal 0.18.1 Apache-2.0, rtrb 0.3.4) measured at 13 net-new packages. Key scheduling consequence: first AUDIBLE sound moves to eng-03 (a tone over the command ring, no node graph), leaving eng-07/MHALL as first MUSICAL sound."
    },
    {
     "ts": "2026-07-20",
     "text": "Rewritten whole after seven discussion rounds (devices, duplex, the FS1R scenario, latency-compensation scope, MIDI delay, first instrument, music-vs-physics). Adds the governing law: the compiler is the last place music exists - the engine receives frequencies, never note numbers, so tuning-awareness is structural. Adds device/stream policy (single-device duplex, output-only recorded, channel zeroing, stream never stops, device loss), latency scope and budget, three proposed requirements (R-310 compensate only what the system introduces; R-311 10 ms live-path budget; R-624 external instrument pairing), and rev-engine's dependency surface reduced to cpal+rtrb only. 21 decisions in its section 18."
    }
   ],
   "created": "2026-07-19",
   "completed": "2026-07-20"
  },
  {
   "id": "eng-02",
   "title": "[checkpoint] Graph runtime node API",
   "kind": "major",
   "track": "engine",
   "status": "complete",
   "depends_on": [
    "eng-01"
   ],
   "requirements": [
    "R-703",
    "R-704",
    "R-707"
   ],
   "links": [
    {
     "doc": "revision_poc.md",
     "anchor": "Padlington port plan"
    },
    {
     "doc": "completed/revision_eng02_proposal.md",
     "anchor": "Decisions requested"
    }
   ],
   "plan": "Node trait/API shape for the Web-Audio-semantics runtime. Human approval before implementation.",
   "verify": "Proposal approved (12 decisions) with four amendments folded in first: the Web Audio CYCLE RULE (legal iff the cycle contains a delay of at least one quantum - we error where the specification mutes) replacing an invented flat rejection; a FIXED 128-FRAME QUANTUM anchored to absolute sample position, which gives block-size-independent determinism, a defined k-rate, and a number for the feedback minimum; decision 9 reworded as sample-accurate onsets with the live-input case named; and live commands to carry a computed sample position rather than only NOW. Implemented in rev-engine: graph.rs (closed NodeKind, GraphSpec -> Graph with topological order, derived channel counts, buffer liveness), voice.rs (Voice + VoicePool), table.rs, param.rs. 293 workspace tests green, clippy -D warnings clean. TWO DEFECTS CAUGHT WHILE WRITING THE TESTS: the render path allocated a Vec of parameter values per node per quantum AND cloned an entire buffer per node; both fixed, and now proven by two tests that render inside RtScope so the allocation guard would fire. The graph moved from Voice to VoicePool as part of that fix - one graph shared by every voice, which is also what lets the node loop hold &Graph and &mut buffers at once. Also replaced an unreachable starvation counter with a STEAL counter, which is the number that actually signals a voice count set too low. FINDING from the property test: tick<->sample round trip is exact only while a tick is at least a sample wide - above roughly 525 bpm at 44.1 kHz, 5040 PPQ is finer than the sample grid and several ticks share a sample. A fact about the resolutions, not a defect; the test now states the weaker fact past that limit and the seed is committed in proptest-regressions.",
   "notes": [
    {
     "ts": "2026-07-21",
     "text": "Taken up after eng-06 rather than before it, by the user's choice: MHALL waits for the node runtime rather than being reached through a minimal placeholder voice. So eng-07 keeps its dependency on eng-05, and the branch is eng-02 -> eng-04 -> eng-05 -> eng-07, with dsp-02/03 for the PadSynth harpsichord alongside. The compiler is already proven against hand-checkable positions, so a wrong note from here on is unambiguously the instrument's fault."
    },
    {
     "ts": "2026-07-21",
     "text": "Proposal drafted: doc/revision_eng02_proposal.md, 12 decisions. Designed against the Padlington inventory rather than an imagined instrument. Headlines: nodes are a CLOSED ENUM not trait objects (a patch is data - R-621/R-103 - and a trait object does not serialize); graphs are described then compiled, with topological order and buffer assignment resolved app-side so the callback only walks a Vec; cycles and channel mismatches are BUILD errors, never silent; a voice is a graph instance from a pre-built pool that returns to the free list when its RELEASE completes, not when its duration expires; stealing distinguishes held from releasing; voices carry a start offset rather than splitting the block; a voice's seed is a pure function of the note and instrument seed, never of its pool slot (a slot-derived seed would make R-1402 fail intermittently). Node census is exactly four kinds - BufferSource, Gain, StereoPanner, Biquad - because that is what the voice being ported uses. eng-04's scope confirmed by census as exactly four ramp methods."
    },
    {
     "ts": "2026-07-21",
     "text": "NOT YET WIRED: the voice pool is not connected to Engine::process, so chunks still are not played. That join is eng-07's, together with splitting the device block into quanta anchored to the session clock. eng-04 owes the four ramp methods before a voice has a real envelope - until then a releasing voice ends at the end of its quantum, which voice.rs says plainly rather than dressing up."
    }
   ],
   "created": "2026-07-19",
   "completed": "2026-07-21"
  },
  {
   "id": "eng-03",
   "title": "cpal duplex bring-up",
   "kind": "major",
   "track": "engine",
   "status": "complete",
   "depends_on": [
    "eng-01"
   ],
   "requirements": [
    "R-301"
   ],
   "links": [],
   "plan": "Open a duplex WASAPI-shared stream (input unused but open); device selection; block-size/format handling; callback skeleton with allocation guard.",
   "verify": "Implemented. First audible sound: `cargo run -p rev-app --bin rev-tone` opens a device and plays a tone; verified on LG TV over HDMI/WASAPI at 48 kHz, 211 blocks, 0 xruns, worst callback 68 us. Offline render of the same tone is bit-identical across two runs (R-1402 gate live before there is anything complicated to render). rev-log writes to one forever SQLite file with stderr echo in debug. Control Bar wired: Play sends Start + an audible tone, Stop sends Stop + Locate(0), and the counter now reads the engine's position snapshot through the app-side tempo conversion rather than a stand-in beat clock. 249 workspace tests green; clippy -D warnings clean; wasm32 check on rev-core green. DEVIATIONS from the proposal, both recorded in code: (1) Block carries a flat planar buffer (PlanarMut) rather than a slice of mutable slices, because the latter cannot be built in a callback without allocating - semantics unchanged; (2) cpal has NO duplex callback (input and output are separate streams even on one device), so the single-device rule of §11.3 is necessary but not sufficient and a real live path will need the two callbacks reconciled against one clock. FINDING: the allocation guard had to exempt itself while std::thread::panicking(), or any panic inside an armed scope double-panics and aborts with no message - found by the guard's own unwind test.",
   "notes": [
    {
     "ts": "2026-07-19",
     "text": "Perf ledger starts here — 'day 1 of first real-time work': callback timing stats (mean/worst/xruns) from the telemetry ring recorded into the historical ledger from this item onward, with build-settings/platform/commit metadata."
    },
    {
     "ts": "2026-07-21",
     "text": "Perf ledger note deferred: callback timing is measured and published in the position snapshot, but entries are not yet harvested into perf/ledger.jsonl. When they are, they must be DEVICE-keyed as well as machine-keyed - callback timing is meaningful on any device, end-to-end latency on shared-mode HDMI is not."
    },
    {
     "ts": "2026-07-21",
     "text": "Closed by the user after running it: transport start/stop, tone, and a counter following the engine's clock, all confirmed on hardware. Post-close additions in the same session: every UI action now logs in prose (WidgetId(3) became 'Loop: on', which needed Kit::label), and the shuttle logs at Trace because it emits a value per pointer move. Defect found and fixed while wiring: PLAY was declared Kind::Button, so it emitted Intent::Released and the entire Play arm had been unreachable since ui-03 - the control had never been a latch, it only looked like one. Play is now Kind::Toggle and reads the engine's running flag each tick, so it reports the transport rather than the pointer."
    }
   ],
   "created": "2026-07-19",
   "completed": "2026-07-21"
  },
  {
   "id": "eng-04",
   "title": "AudioParam automation module",
   "kind": "major",
   "track": "engine",
   "status": "complete",
   "depends_on": [
    "eng-02"
   ],
   "requirements": [
    "R-704"
   ],
   "links": [],
   "plan": "setTargetAtTime/ramp math per W3C normative formulas, unit-tested against spec examples. Shared by every voice ever ported.",
   "verify": "Implemented in rev-engine/automation.rs: the four methods the Padlington census found (setValueAtTime, linearRampToValueAtTime, exponentialRampToValueAtTime, setTargetAtTime) against the W3C formulas, plus cancel_from and reset. Fixed capacity (8 events), allocation-free, insertion-sorted so out-of-order scheduling is permitted as the specification allows; overflow is counted, never silent, because a note-off schedules a release ON THE AUDIO THREAD and a dropped release is audible. Time is in frames since the voice started - seconds never appear below the seam. Evaluated in closed form rather than as a one-pole recursion: the recursion is the usual optimization but carries its own state and is not the same function at every rate, and at 68us of a 10ms budget the exact form costs nothing we have. Wired through: Param carries an Automation; a parameter with nothing scheduled skips evaluation entirely. Voices now hold their OWN parameter copies reset at note-on - a shared schedule would make a voice's sound depend on what it played before - and carry a frame counter. The release is now a real length (VoicePool::set_release), so a voice is reclaimed when its TAIL is over rather than at the end of the quantum, which was the placeholder eng-02 left. 311 workspace tests green, clippy -D warnings clean. SPEC CORNER FLAGGED, NOT GUESSED: what a ramp following a setTarget ramps FROM. Our reading - the ramp starts from the value at the previous event's time, so the target's progress is ignored - is asserted in a test and documented as unverified against the specification text. It does not arise in the voice being ported (attack ramp, decay target, release target; no ramp follows a target).",
   "notes": [
    {
     "ts": "2026-07-21",
     "text": "The exponential ramp's special case is implemented and tested: if the start value is zero or the two values have opposite signs, the value HOLDS and then jumps rather than interpolating through zero. Easy to miss, audible when missed, and the alternative is a NaN."
    }
   ],
   "created": "2026-07-19",
   "completed": "2026-07-21"
  },
  {
   "id": "eng-05",
   "title": "First nodes + sine voice",
   "kind": "major",
   "track": "engine",
   "status": "complete",
   "depends_on": [
    "eng-02",
    "eng-04"
   ],
   "requirements": [
    "R-704"
   ],
   "links": [],
   "plan": "Buffer-source (loop + playback_rate) and gain nodes; a sine voice as the first graph description.",
   "verify": "Implemented as rev-engine/instrument.rs: the Padlington voice transcribed from the inventory section 4 - two seeded read heads, 1/sqrt(2) trims, opposed panners, amp gain, lowpass - with Patch holding only the PLAY-TIME parameters (the bake-relevant set stays with dsp-02, preserving the partition the inventory validated). Envelopes are the inventory's math: a LINEAR amplitude attack (exponential from near zero is inaudible then snaps), a decay target toward sustain, a release target; the filter takes the same shape in octave space via an EXPONENTIAL ramp, with key tracking measured from a reference_hz that becomes the tuning's anchor when tunings reach here. The JS-ism of clamping the attack against the duration is retired: R-402a means the duration is known up front. Also completed eng-02's unfinished multi-input summing - the voice needs it, two panners into one gain - with nodes now operating IN PLACE on a gathered output buffer. 325 workspace tests green, clippy -D warnings clean. TWO REAL BUGS FOUND BY THE TESTS: (1) the instrument recorded SPEC node indices while voices index parameters by WIRING position, and the topological sort reorders - so envelopes were landing on whichever node happened to sort into that slot, silently. Graph now exposes position_of and the translation is explicit. (2) Buffers are recycled WITHIN a pass, and a recycled buffer still held the earlier node's output; accumulating into it added a stale signal that bypassed everything in between - a filter fed by a near-silent envelope was still loud because it had inherited a panner's output. Buffers are now cleared at their node's turn, and there is a regression test whose graph reproduces the shape.",
   "notes": [
    {
     "ts": "2026-07-21",
     "text": "A test of mine was wrong too, and worth recording: it measured decay by taking the peak of a SPAN, which for a decaying sound is the start of that span - so it compared the wrong instants. Now measured as one quantum at three points."
    }
   ],
   "created": "2026-07-19",
   "completed": "2026-07-21"
  },
  {
   "id": "eng-06",
   "title": "Schedule compiler (reads v_realized)",
   "kind": "major",
   "track": "engine",
   "status": "complete",
   "depends_on": [
    "core-03",
    "eng-01"
   ],
   "requirements": [
    "R-003",
    "R-302"
   ],
   "links": [
    {
     "doc": "completed/revision_eng06_proposal.md",
     "anchor": "Decisions requested"
    }
   ],
   "plan": "Compile v_realized ORDER BY at_tick through the tempo map into sample-stamped event chunks; look-ahead window feeding the command ring.",
   "verify": "Implemented. New crate rev-sched (core+store+engine, no new external dependency). TempoMap converts tick<->sample in i128, segment-anchored so boundaries are computed once and error cannot compound, round-half-to-even so nothing biases late; both directions live there, so the counter and the roll cannot disagree with the compiler. Compiler turns v_realized windows into Chunks of Notes carrying at/dur/hz/level/voice - frequencies resolved through the tuning at compile time (R-312), durations never truncated by a window (R-405a). The chunk payload deferred at eng-01 section 8 is now filled in without any change to the ownership discipline, which is the evidence the deferral was right. 18 tests: MHALL compiles to hand-checkable positions (quarter = 24000 samples at 120bpm/48kHz), E4 = 329.63 Hz, velocity 49152 -> level 0.75, consecutive windows partition the tune with nothing dropped or duplicated, the final whole note outlives its window, compiling twice is bit-identical, a tempo change moves positions and durations together, and the 16-ET party trick moves every frequency and no position. Two proptests: conversion is monotonic under any tempo map and rate, and a tick survives the round trip. 267 workspace tests green, clippy -D warnings clean, file map current. DECIDED DURING IMPLEMENTATION: a phrase's tuning is optional (R-414), so an untuned phrase's notes resolve through the PROJECT DEFAULT tuning from meta - a project-level fallback, explicitly not inheritance from a parent phrase. NOT YET DONE: the engine takes a chunk but does not yet play its notes; dispatch needs a voice pool, which is eng-07 plus a minimal voice.",
   "notes": [
    {
     "ts": "2026-07-21",
     "text": "Proposal drafted: doc/revision_eng06_proposal.md, 10 decisions. Pays eng-01's deferred chunk-payload debt. Headlines: a new rev-sched crate (the compiler cannot live in rev-app because eng-07 is headless, nor in rev-engine because R-312 forbids it); integer-exact segment-anchored tick->sample in i128 replacing f64 on the compile path, for monotonicity and cross-platform determinism; notes carry DURATION rather than paired on/off events, which makes superseding a schedule incapable of orphaning a note-off; chunks stamped in PLAY-POSITION space so looping needs no recompilation. Tuning resolution lives in this crate and is shared by the live path and the roll, so they cannot diverge."
    },
    {
     "ts": "2026-07-21",
     "text": "Proposal APPROVED 2026-07-21, all decisions as recommended, with two amendments folded in before approval: section 5.1 grounds duration-carrying notes as a modelling claim (the model already says a note has a duration; on/off is a wire encoding, and the principle is 'pairs at the edges, entities in the middle'), and section 5.2 removes the unbounded-note sentinel entirely - a continuously sounding source is instrument state expressed by automation, not a note without an end. That keeps the voice pool's reclamation invariant total and makes a stuck note a diagnosable bug rather than a legal state. R-402a written into the requirements."
    },
    {
     "ts": "2026-07-21",
     "text": "Store gained query::realized_between (windowed by onset) for the look-ahead. Onsets only: a note beginning before the window is not returned even if it would still be sounding. That is deliberate and is the same rule R-405a applies to material - reconstructing what should already be sounding at an arbitrary position is CHASING, a real feature and a separate one, and it is also what makes locating into the middle of a held note work. Filed as speculative, not planned."
    }
   ],
   "created": "2026-07-19",
   "completed": "2026-07-21"
  },
  {
   "id": "eng-07",
   "title": "MHALL headless — first sound",
   "kind": "major",
   "track": "engine",
   "status": "complete",
   "depends_on": [
    "eng-03",
    "eng-05",
    "eng-06"
   ],
   "requirements": [
    "R-706",
    "R-1402"
   ],
   "links": [],
   "plan": "Mary Had a Little Lamb from a stored phrase, no UI. Offline render of the same schedule; render-twice bit-identity gate. Stage 2 exit.",
   "verify": "MHALL SOUNDS, confirmed by the user on hardware. The whole chain: store -> v_realized -> compiler -> tuning -> chunk -> command ring -> engine -> voice pool -> device. 1900 blocks, 0 xruns, worst callback 609 us against a ~10 ms budget. `cargo run -p rev-app --bin rev-mhall`, with --tuning/--bpm/--device/--render (which renders twice and compares). Engine::process now renders in session-anchored quanta and dispatches chunk notes at sub-quantum offsets. 331 workspace tests green, clippy -D warnings clean. TWO DEFECTS THE USER HEARD THAT NO TEST CAUGHT: (1) read-head start offsets were built as (scramble >> 11), up to 2^53, where an f64's ULP is 2 - adding a per-sample increment of ~3.3 quantized to garbage, so the read SPEED and therefore the PITCH came out wrong, differently for every seed. Bit-identity passed because wrong is reproducible; onsets passed because timing was fine. NOTHING WE HAD WRITTEN LISTENS TO PITCH. (2) the stand-in table was a naive ramp, full energy to Nyquist, played back 2.6-3.9x faster than written and folding all of it down as aliasing; now built additively from 48 harmonics. ACTION: dsp-02 should add a spectral test that measures a rendered note's fundamental - it needs an FFT anyway.",
   "notes": [
    {
     "ts": "2026-07-21",
     "text": "KNOWN GAP, left as an ignored failing test (sound.rs: the_device_block_size_is_not_audible_even_when_voices_are_stolen): renders diverge across device buffer sizes once voices are STOLEN. Two determinism bugs were fixed getting here - k-rate parameters evaluated per call rather than per quantum, and voice state transitions that let reclamation order depend on how the device chopped up time - but something in the stealing path still does it. Suspicion: which slot a stolen note lands in can differ, and voices are summed in slot order, and floating-point addition is not associative. Matters for R-1402 whenever an arrangement is dense enough to steal."
    }
   ],
   "created": "2026-07-19",
   "completed": "2026-07-21"
  },
  {
   "id": "midi-01",
   "title": "[checkpoint] MIDI crate API + clock correlation",
   "kind": "major",
   "track": "midi",
   "status": "planned",
   "depends_on": [
    "eng-01"
   ],
   "requirements": [
    "R-603"
   ],
   "links": [
    {
     "doc": "revision_poc.md",
     "anchor": "Timing engine sketch"
    }
   ],
   "plan": "midir wrapper API; OS-timestamp to sample-clock correlation approach (dedicated module + tests). Human approval before implementation.",
   "verify": "",
   "notes": [],
   "created": "2026-07-19",
   "completed": null
  },
  {
   "id": "midi-02",
   "title": "Enumeration + timestamped input + thru fast path",
   "kind": "major",
   "track": "midi",
   "status": "planned",
   "depends_on": [
    "midi-01",
    "eng-07"
   ],
   "requirements": [
    "R-601",
    "R-603",
    "R-605"
   ],
   "links": [],
   "plan": "Hot-plug enumeration; driver-boundary timestamps; the dual-path fork (thru ring to engine + event path to app).",
   "verify": "",
   "notes": [],
   "created": "2026-07-19",
   "completed": null
  },
  {
   "id": "midi-03",
   "title": "Live playthrough + honest latency print",
   "kind": "major",
   "track": "midi",
   "status": "planned",
   "depends_on": [
    "midi-02"
   ],
   "requirements": [
    "R-306",
    "R-307"
   ],
   "links": [],
   "plan": "Play the voice from the Code/Oxygen; measure and print end-to-end latency (R-307 v0). Stage 3 exit. Stretch: one knob mapped to a voice macro.",
   "verify": "",
   "notes": [],
   "created": "2026-07-19",
   "completed": null
  },
  {
   "id": "ui-01",
   "title": "[checkpoint] Mechanism contract as API + widget kit shape",
   "kind": "major",
   "track": "ui",
   "status": "complete",
   "depends_on": [
    "boot-02"
   ],
   "requirements": [
    "R-907",
    "R-921",
    "R-928",
    "R-1514",
    "R-1515"
   ],
   "links": [
    {
     "doc": "revision_poc.md",
     "anchor": "Mechanism contract v0 sketch"
    },
    {
     "doc": "completed/revision_ui01_proposal.md",
     "anchor": "ui-01 proposal"
    }
   ],
   "plan": "Finalize the mechanism contract sketch as concrete API (events, focus, capture, hands-off clause, text channels); widget-kit API shape. Human approval before implementation.",
   "verify": "All fifteen decisions approved as recommended, plus the six amendments in the proposal's §13. Deliverable is the document: rev-ui-mech's API (windows keyed, per-window DPI/dirty/focus/cursor/a11y, implicit pointer capture, keyboard-vs-text channels, the hands-off clause with begin_relative_drag as the one sanctioned warp, paint list, text shaping with caret/hit-test, drag sessions with type negotiation) and rev-ui-kit's shape (slot tree, retained widget tree, intents, skin, anchored layout). Renderer settled as CPU (softbuffer + tiny-skia). Six invariants adopted so the single-document first pass is N=1 rather than single-document code. Dependencies added and the workspace builds clean: 112 third-party packages on windows, no MPL, no BSD-4, one copyleft arm (self_cell Apache-2.0 OR GPL-2.0-only) declined in favour of Apache and recorded in deny.toml. Types are written in ui-02 where the compiler participates — §13.5 records that the §7 Host sketch does not compile as drawn.",
   "notes": [
    {
     "ts": "2026-07-20",
     "text": "Proposal drafted 2026-07-20, rewritten same day after the multi-window / multi-document discussion. Renderer settled as CPU (softbuffer + tiny-skia) with a paint-list seam; 70-package dependency census measured; six invariants adopted so the single-document first pass is N=1, never single-document code. Consequences flagged for eng-01 (session-keyed engine), core-01 (content identity), and a requirements block R-921+."
    },
    {
     "ts": "2026-07-20",
     "text": "Amendments settled at approval: bundled fonts (Source Sans 3 Regular/Italic/Bold/BoldItalic, JetBrains Mono Regular/Bold, both OFL-1.1) because bit-identical screenshots are impossible with system fonts; Lucide icons (ISC) as build-time generated paths, avoiding usvg's MPL-2.0; hand-drawn transport glyphs; asset manifest, which forced R-1514's rewording and produced R-1515; vsync overclaim in §7.1 corrected; testdata/ui/ for screenshot masters."
    }
   ],
   "created": "2026-07-19",
   "completed": "2026-07-20"
  },
  {
   "id": "ui-02",
   "title": "Window/surface/text bring-up",
   "kind": "major",
   "track": "ui",
   "status": "complete",
   "depends_on": [
    "ui-01"
   ],
   "requirements": [
    "R-907",
    "R-928",
    "R-938",
    "R-1507"
   ],
   "links": [
    {
     "doc": "completed/revision_ui01_proposal.md",
     "anchor": "9. ui-02 exit criteria"
    }
   ],
   "plan": "Seven ordered steps; three of them end at something the user can look at, which is how UI gets verified here. Exit criteria are the proposal's §9.\n\n1. ASSETS. Vendor Source Sans 3 (Regular/Italic/Bold/BoldItalic) and JetBrains Mono (Regular/Bold) from upstream project releases into asset/font/, each with its OFL text. Read the OFL headers for a Reserved Font Name before anything is modified; no subsetting for now. Write asset/asset.json (path, source URL, version, checksum, licence, author) — the manifest R-1514 reads and R-1515 credits from. File-map entry lands with it.\n\n2. WINDOW AND SURFACE. winit ApplicationHandler; windows held in a WindowId-keyed map, never a field. softbuffer context and surface per window; resize, scale-factor change (a window dragged between monitors), and close all handled. THE BORROW SHAPE IS RESOLVED HERE (proposal §13.5): Mech cannot both own the Host and be handed to it. Preference, to be argued with the compiler: split into a driver that owns the loop and a services handle passed to callbacks; fall back to host-returns-requests if that fights the borrow checker. Deliverable: a window of a solid colour that resizes correctly. USER EYEBALLS IT.\n\n3. PAINT. One tiny-skia Pixmap per window, sized in physical pixels, with a global scale transform so all widget geometry stays logical. Implement the §7.4 vocabulary. Detail that matters: tiny-skia is premultiplied RGBA8 and softbuffer wants 0RGB u32 — the window is opaque, so the bridge drops alpha rather than unpremultiplying, and that assumption is written down where it is made. Deliverable: rects, rounded rects, lines, a path. USER EYEBALLS IT.\n\n4. DIRTY. A per-window dirty region as a small union of rects, degrading to full-frame past a cap; painting clipped to it; redraw requested only when non-empty, coalesced once per event-loop wakeup (§13.4 — softbuffer has no vsync, and the earlier claim of vsync-timed frames was withdrawn). Verified by a static frame doing no work.\n\n5. TEXT. cosmic-text FontSystem and SwashCache; a role table (FontRole::Ui, FontRole::Numeric) resolving to the bundled faces so no widget names a file; ligatures and contextual alternates off; system fallback allowed at runtime, pinned off under test. The glyph bridge — swash's rasterized image blitted into the Pixmap — is the least specified part of the design and the second real risk. Detail that matters: grayscale antialiasing, not subpixel/LCD, because subpixel output depends on the physical panel and would make bit-identical screenshots meaningless. Shaped::size/caret/byte_at ship here; the Counter needs all three. Deliverable: the Control Bar's real strings — counter digits, Play/Stop/Rec/Loop, a couple of long track names — in both faces. USER EYEBALLS IT, and it doubles as the font comparison.\n\n6. INPUT. Pointer and key routing per §7.2 with implicit capture (a drag that leaves the window still tracks), hover enter/leave, wheel to the hovered target, the keyboard-vs-TextInput split, focus ownership with Reason, cursor requests, and begin_relative_drag. Hands-off probes for the three laws (no focus steal, no pointer warp, no unbidden scroll).\n\n7. PROBE AND MASTER. Open and close a second window at runtime — the anti-rot test for invariants 3 and 4, deliberately exceeding N=1. Capture a Pixmap to PNG, commit the first reference under testdata/ui/, and add the bit-identical comparator to rev-testkit with fallback pinned. File-map description for testdata/ stops claiming audio-only.\n\nNOT IN THIS ITEM: widgets of any kind (ui-03), slot-tree interaction (splitters, docking, tear-off), a11y population, IME, drag transport, GPU anything.",
   "verify": "All eight exit criteria met, seven by test and the eighth by the user's eye. Window opens/resizes/closes; a second window opens and closes at runtime at its own scale (the anti-rot test for invariants 3 and 4); the paint vocabulary renders rects, rounded rects, lines, paths and shaped text; pointer and keyboard route with implicit capture demonstrated by a drag that leaves the window and still tracks; dirty regions plumbed per window and a frame with nothing dirty does not repaint (hovering one button repaints ~76x30, not the window); the hands-off laws hold, with begin_relative_drag the one sanctioned warp; two PNG golden masters committed and compared bit-identically. 110 tests, fmt, clippy -D warnings, wasm check, filemap and schema all green. Two bugs the user caught by looking: DPI deferred for lack of a second monitor (single-monitor test is Windows display scaling), and button paint precedence, fixed and generalized into a skin rule for ui-03. One store bug found by proptest during this item and fixed: journal::next_redo. Tail work closed three gaps found by review rather than by failure: the missing accessibility slot, cursor requests that only landed on a repaint, and pixel snapping for fractional interface scales (with a 1.25x golden master added, because the 1x and 2x references could not have caught it).",
   "notes": [
    {
     "ts": "2026-07-20",
     "text": "Started 2026-07-20. Dependencies landed with ui-01's approval; workspace builds clean. Assets (fonts, icons) are vendored as part of this item."
    },
    {
     "ts": "2026-07-20",
     "text": "Plan written as seven ordered steps before any code. Steps 2, 3, and 5 each end at a user-eyeballed deliverable. The two acknowledged risks are the Host/Mech borrow shape (step 2) and the cosmic-text-to-tiny-skia glyph bridge (step 5)."
    },
    {
     "ts": "2026-07-20",
     "text": "Steps 1-2 done. Fonts vendored (Source Sans 3 3.052R, JetBrains Mono NL 2.304) with OFL texts and asset/asset.json carrying sha256/source/version/author/role. Finding: Source Sans DOES declare a Reserved Font Name ('Source', an Adobe trademark) - so subsetting would require renaming; recorded per entry. Took the NL (no-ligature) build of JetBrains Mono so ligatures are absent from the font rather than suppressed by a shaper flag. Borrow shape resolved as predicted: a private Driver owns mech and host as sibling fields, so callbacks borrow both disjointly; window creation is queued because ActiveEventLoop only exists inside a callback. rev-app opens a window that paints a DPI-scaled panel. 79 tests, fmt, clippy, wasm all green."
    },
    {
     "ts": "2026-07-20",
     "text": "Step 3 done: Painter over tiny-skia with clear/fill_rect/fill_round_rect/stroke_line/fill_path plus nesting offset and clip stacks; Color and Outline/Path wrappers so no tiny-skia type is nameable above this crate. Rect clips resolve analytically; only paths and strokes build a Mask, cached because consecutive draws share a clip. Blit is premultiplied RGBA8 -> softbuffer 0RGB, dropping alpha because the window is opaque - documented at the one site that would have to learn otherwise. 6 painter tests."
    },
    {
     "ts": "2026-07-20",
     "text": "Unrelated find during the step-3 gate run: a store property test failed on a fresh seed with a FOREIGN KEY violation, shrunk to [Undo, Undo, CreatePhrase, CreatePhrase, SetTuning]. Two undos reverse the world gesture and then genesis, deleting the builtin tunings; the harness still referred to 16-ET. Harness bug, not a store bug - the same class the track guard already handled, now guarded for tuning too. Counterexample committed to tests/proptest-regressions/property.txt."
    },
    {
     "ts": "2026-07-20",
     "text": "Step 4 done: Dirty is a capped list of logical rects (CAP 8, collapsing to bounds past it), per window. mark_dirty(id, rect) and mark_dirty_all(id); painting is clipped to the region's bounds and clear() honours the clip, so partial frames keep last frame's pixels - correct because the Pixmap persists between frames. Frame carries &Dirty so widgets can skip subtrees via touches(). Known limit: the blit and present are still full-window, because softbuffer does not guarantee the back buffer is preserved between frames; the rasterization saving (the expensive half) is real. 7 dirty tests."
    },
    {
     "ts": "2026-07-20",
     "text": "REAL STORE BUG found by proptest during the step-4 gate run, in journal::next_redo. The redo target was 'the lowest currently-undone gesture', with stack invalidation detected by the last journal entry not being a marker. That check is necessary but insufficient: after a new command invalidates the stack, a later undo puts a marker back at the end, and MIN(undone) then reaches PAST the new command to a stale gesture. Redo replayed a payload whose rows the intervening history had moved - symptom was NotFound on a row id. Fixed: redo undoes the last undo, computed by replaying the marker run after the last command entry (undo pushes, redo pops). Regression test a_closed_redo_stack_stays_closed_after_a_later_undo added and verified to FAIL against the old implementation before being kept."
    },
    {
     "ts": "2026-07-20",
     "text": "Step 5 done: text stack. FontRole::{Ui,Numeric} resolves to the bundled families; no widget names a file. Fonts are embedded with include_bytes! so the binary is self-sufficient and every machine rasterizes the same shapes; the locale is pinned to en-US rather than the machine's, since locale steers fallback and a golden master must not depend on regional settings. System fallback on at runtime, off under test. Shaping is scale-free (logical) and rasterization is scaled, so a shaped run survives a DPI change. Grayscale AA only - subpixel depends on panel stripe order and would make bit-identical screenshots meaningless. Glyph coverage is blended source-over per pixel with the clip tested inline (a mask would cost a buffer-sized allocation to spare a few comparisons). 11 new tests: monospace/proportional roles proven by advance width, real italic and bold faces (not synthesized obliques), caret/byte_at round-trip, clip containment, and that glyph edges blend rather than replace."
    },
    {
     "ts": "2026-07-20",
     "text": "Step 6 done: input routing plus interface scale (R-938, new requirement). Implicit capture is structural - a press binds its target until release, wherever the pointer goes, so a widget cannot forget it. Wheel targets the hovered widget, not the focused one. Enter always follows a Leave, never two enters running. Activation is not focus: a press operates a control and deliberately does not move focus or raise the window. Raw keys and composed text are two channels; control characters are keys only, never text. begin/end_relative_drag hides and locks the pointer, delivers raw device deltas (the only honest source for infinite drag - it keeps reporting past a screen edge), and restores the pointer on release: the single sanctioned exception to the hands-off clause, offered BY the mechanism so no widget rolls its own warp. Cursor shape is requested per frame and resets, so a widget that stops asking gets the default back. R-938 scale composes with platform DPI (multiplies) rather than replacing it, is clamped 0.5-4.0, and takes an optional per-window override that REPLACES the workspace default rather than compounding. Cheap because invariant 4 had already forced DPI through a single site. Routing lives in mech/route.rs (child module: reaches the driver's internals, keeps mech.rs at 537 lines)."
    },
    {
     "ts": "2026-07-20",
     "text": "Step 6 follow-up: user caught a harness bug - clicking an off button left it looking off until the cursor left it. Cause was paint precedence (hover checked before latched state), not routing. Fixed by modulating the base colour rather than replacing it; the general rule is recorded against ui-03."
    },
    {
     "ts": "2026-07-20",
     "text": "Step 7 done. Canvas: offscreen painting with the same paint list and no window, font stack in no-system-fonts mode - public, because offscreen render and thumbnails want it too. rev-testkit::image compares PNG bit-identically; a missing reference is written and then FAILS rather than passing quietly (a master nobody looked at is not a master), and a differing render is written as .actual.png beside it so the two can be compared by eye. REVISION_BLESS=1 regenerates, deliberately. Three golden tests: the scene at 1x and 2x (where a stored or double-applied scale factor would show) and same-run reproducibility. Second-window probe on 'n', opened at 1.5x WINDOW scale so per-window scale is proven distinct from the workspace default; closing the probe leaves the app running, closing the main window quits. .actual.png added to .gitignore."
    },
    {
     "ts": "2026-07-20",
     "text": "Two bugs found by the user's first press of the probe key, both real and both in the mechanism rather than the harness. (1) Synthetic key events were being delivered. winit resynthesizes presses for keys still physically held when focus moves, so opening a window with a key immediately produced a second 'press' of that key and the new window's own focus change undid the action that created it. Any key that opens or closes a window would have had this. Now dropped at the boundary: they are platform bookkeeping, not user intent. (2) set_window_scale on a queued window silently did nothing, because window creation is deferred until the loop drains requests - so the probe would have opened at workspace scale while claiming 1.5x. Initial scale moved into WindowSpec where it is applied at creation; set_window_scale is now documented as being for windows that exist."
    },
    {
     "ts": "2026-07-20",
     "text": "Tail of ui-02, three gaps closed before widgets exist. (1) The accessibility slot ui-01 promised was missing - it was in the first draft of host.rs and did not survive a rewrite. Now a11y.rs: per-window Tree of Nodes keyed by the SAME TargetId input routes to, so pointer and assistive technology can never drift apart; roles cover the Control Bar census; a node must state its label, value and bounds, which is precisely the smell it exists to catch (a control whose name lives only in painted pixels cannot answer). Declared, not bridged - no AccessKit dependency yet. The harness implements it, proving the shape works on a real interface. (2) Cursor requests were applied only at the end of paint, so a widget that requested one without invalidating anything silently got nothing; now applied after event dispatch. (3) Pixel snapping for axis-aligned fills and strokes, which R-938's fractional scales made necessary: odd-width rules centre on x.5, even on a boundary, diagonals untouched, and a sub-pixel rect keeps one pixel rather than vanishing. Adding it moved no golden master - which exposed that both references used integer scales and so could not see snapping at all; a third reference at 1.25x now covers it. 118 tests."
    }
   ],
   "created": "2026-07-19",
   "completed": "2026-07-20"
  },
  {
   "id": "ui-03",
   "title": "Widget kit: Control Bar census widgets",
   "kind": "major",
   "track": "ui",
   "status": "complete",
   "depends_on": [
    "ui-02"
   ],
   "requirements": [
    "R-712",
    "R-907",
    "R-938",
    "R-939",
    "R-940"
   ],
   "links": [
    {
     "doc": "revision_poc.md",
     "anchor": "Two sources, and what each governs"
    },
    {
     "doc": "revision_poc.md",
     "anchor": "Why the Control Bar slice"
    },
    {
     "doc": "revision_skin_inventory.md",
     "anchor": "Skin inventory"
    }
   ],
   "plan": "Six steps. The Control Bar's FUNCTION comes from Vision; its LOOK comes from the Notorolla exhibits (revision_poc.md, \"Two sources\"). Not a pixel-for-pixel reconstruction of 1998.\n\n1. PAINT VOCABULARY. The prerequisite, not a polish pass: the exhibits are built from linear gradients (every slider cap is a three-stop gradient), inset shadows (the recessed slot), drop shadows and glow, and rev-ui-mech paints flat fills only. Add linear/radial gradient shaders (tiny-skia provides them), plus inset and outer shadow - which means a blur, the one piece with no library behind it. Golden masters extend to cover each new primitive at 1x and 1.25x.\n\n2. SKIN. One Skin value holding every colour, metric and radius, derived from a panel-lightness scalar the way the exhibits derive theirs by calc(). Two semantic colour systems layered on the same primitives: ROLES for parameters (LFO rust 15 / Oscillator orange 30 / Filter green 120 / Envelope cyan 190 / Effects blue 215, an absent role leaving a deliberate spectrum gap), STATES for transport (amber readout, red record accent, blue active/armed). Type sizes start at 16-18px for labels against the user's 14px legibility floor - larger than control-skin convention, because the target display is a 50-inch 4K panel at desk distance.\n\n3. WIDGET FOUNDATION. Retained tree with stable ids; Widget trait; intents; anchored layout; the state-precedence rule (intrinsic state supplies the base colour, interaction state only modulates it - a control must never lie about itself while the pointer rests on it). Every widget answers the a11y contract: label, value, bounds.\n\n4. CONTROL BAR CENSUS, incrementally, user eyeballs each: tri-state button (Record: highlighted / flashing-armed / disabled) FIRST, because three intrinsic states crossed with two interaction states is where step 3's rule bites hardest; then pop-up, toggle, multi-field numeric display (click-type and click-drag, editable during playback), shuttle, locator bank.\n\n5. INTERACTION, from the exhibits' settled behaviour: pointer capture on press (already structural in the mechanism); sliders jump-to-position with a detent snap window; knobs drag relatively; wheel coarse, tilt fine; double-click recentres to the detent; double-click a readout to type an exact value, and typed values do not snap.\n\n6. ASSEMBLY. The strip laid out horizontally, wired to nothing yet - ui-04 connects it to the engine.\n\nNOT IN THIS ITEM: the parameter-panel builder (generating panels from R-712 declarations, skinned by role, consumed later by R-622 device panels and R-1310 script panels) - its own item, sharing steps 1-3.",
   "verify": "All six steps done, each eyeballed by the user. Paint vocabulary (linear gradients, blurred outer/inset shadows on rounded rects, pixel snapping) with an exact-table box blur at 11.6 ns per mask pixel - 2.9x faster than the first cut, arrived at by measuring rather than guessing, and with the transpose optimization tried, measured worse, and documented as rejected. Skin derived from one panel-lightness scalar as the exhibits derive theirs, with roles for parameters and states for transport. Widget tree made of DATA rather than closures, so R-622 device panels, R-1310 script panels and a layout designer all remain reachable; design and interaction are separate and a test asserts the design is byte-identical across a hover. Census complete: tri-state record with a kit-owned blink phase, multi-field counter with click-drag and typing, locators, pop-up with an overlay pass, shuttle, slider. Interaction transcribed from the exhibits into a `feel` module. 194 tests, six golden masters compared bit-identically at 1x/1.25x/2x, fmt, clippy -D warnings, wasm, filemap and schema all green. Nine defects were found by the user OPERATING it, none by a test: cancelled presses reporting as releases, Play's latch invisible, locators unclearable, the counter's selection invisible, the wheel acting on the clicked field rather than the hovered one, no wheel affordance at all, no provisional highlight in menus, two blue rows in an open menu, and pressed controls staying lit when dragged off. The last of those was found by reading Apple's 1992 guidelines, which also became R-939's normative authority.",
   "notes": [
    {
     "ts": "2026-07-20",
     "text": "Type sizing constraint from the user's setup (50-inch class 4K TV at desk distance, established while eyeballing ui-02 step 5): 14px is the FLOOR for text meant to be casually readable; 18px is a comfortable small size. The skin's defaults are chosen against that, not against a 27-inch desktop monitor - high DPI on a large panel viewed from further away inverts the usual assumption. Sizes below 14px are for dense, deliberately-scanned material only."
    },
    {
     "ts": "2026-07-20",
     "text": "Fonts settled at ui-02 step 5 after eyeballing: Source Sans 3 (ui role) + JetBrains Mono NL (numeric role) accepted."
    },
    {
     "ts": "2026-07-20",
     "text": "Skin rule, learned from a bug the user caught in the ui-02 harness: interaction state MODULATES intrinsic state, it never replaces it. Painting hover as its own flat colour made a latched button read as unlatched for as long as the pointer rested on it - the control lied about itself while you were looking at it, which is the worst possible moment. The kit's visual model is therefore: base colour from what the control IS (on/off/disabled/armed), then a lift or tint for what the pointer is DOING (hover/press). This matters more for the tri-state Record button (highlighted / flashing-armed / disabled) than anywhere else, where three intrinsic states cross two interaction states."
    },
    {
     "ts": "2026-07-20",
     "text": "Design sources settled: Vision's Control Bar supplies FUNCTION (which controls exist, what they do, the behavioural laws, layout where it was good); ../Notorolla/future/ui_skin supplies the VISUAL LANGUAGE for the whole product, Control Bar included. Explicitly not a pixel-for-pixel reconstruction - Vision's monochrome look, abbreviations and modality were 1998 constraints wearing the costume of decisions. The exhibits' role-hue system is a parameter taxonomy and does NOT transfer to a transport, which has no roles; what transfers is the role-independent vocabulary (amber tabular readouts, red record accent, blue active, panel-lightness scalar, machined cap and recessed slot). Notorolla stays read-only: the spec is read there and reimplemented here."
    },
    {
     "ts": "2026-07-20",
     "text": "Step 1 (paint vocabulary) done. Fill{Solid,Linear} taken by impl Into<Fill>, so no existing Color call site changed; radial cut for want of a caller. Gradients anchor to the requested shape, not the clipped one, so a partially dirty frame cannot reshade a widget. shadow_round_rect: outer and inset, three-pass box blur on 8-bit coverage, integer arithmetic so it is bit-identical everywhere. Two golden scenes added (chrome_1x, chrome_125x) showing recessed slots, gradient caps with drop shadows, a panel with outer shadow, and a lamp glow. Arc helper deferred until the first knob asks for it, per the user's own YAGNI rule. Observation worth keeping: many CSS 'inset shadows' in the exhibits are 1px hairlines, not blurs, and need no new primitive at all."
    },
    {
     "ts": "2026-07-20",
     "text": "MEASUREMENT (the cache question, answered with numbers rather than assumption). A 400x160 panel with 12 shadows: dev 18.6 ms, release 3.55 ms, 83k mask pixels; at 1.25x, 130k pixels and 6.06 ms release. Two conclusions. (1) 12 shadows span only 4 DISTINCT geometries - a 3:1 repetition ratio in a toy scene, and a real skin repeats far harder (a bank of identical sliders), so a cache keyed on (w,h,radius,sigma) is justified. (2) But 43 ns per mask pixel is itself high for six running-sum passes, so the per-pixel cost deserves a look before or alongside caching - the mask is allocated and zeroed per shadow, and the composite loop tests the clip per pixel. Recommendation: profile the blur first, then cache; caching a slow primitive hides the slowness instead of fixing it. Separately, rev-ui-mech now builds at opt-level 2 in dev (boot-02 anticipated raising this empirically): 18.6 ms of blur per repaint made the harness sluggish to work with."
    },
    {
     "ts": "2026-07-20",
     "text": "Inventory done: doc/revision_skin_inventory.md, a read-only transcription of the CHOSEN designs only (skin-e-composite plus the seven locked instrument exhibits and the FM-algorithm control). The Round-1-to-4 studies are excluded deliberately - rejected candidates describe roads not taken. Covers the standing laws (lights glow text never; mono readouts in fixed windows; one weight; density from removing dead space), the derived token table, the locked role hues and canonical order, the layout rules, all eight widget archetypes with their specs, and the settled interaction. Flags what our paint list still lacks for full fidelity (radial gradients for knob bodies, annular arcs for value rings - neither has a caller until step 4) and three open questions for the skin."
    },
    {
     "ts": "2026-07-20",
     "text": "BLUR COST, measured then fixed. Started at 33 ns per mask pixel. Transposing between passes so the vertical blur runs sequentially - the obvious cache optimization - measured WORSE (43 ns/px) and was reverted: a shadow mask is a few thousand bytes and already in L1, so there was no stride penalty to avoid and the transposes were pure added work. The real cost was the integer division per pixel per pass, six per pixel. Replaced with an exact quotient table (sums are at most 255*window, a few thousand), giving 12.4 ns/px - a 2.9x speedup - with the golden masters still matching bit for bit, which is what proves the table exact rather than approximate. A 12-shadow panel now costs 0.63 ms/frame wall, 0.33 ms of it blur. CONCLUSION ON THE CACHE: not yet. At this cost a full repaint of a realistic Control Bar is a couple of milliseconds, and dirty regions mean full repaints are rare. The instrumentation stays; revisit if a real panel measures badly. The rejection is recorded in blur.rs so nobody re-derives the transpose idea from first principles."
    },
    {
     "ts": "2026-07-20",
     "text": "Step 2 (Skin) done in rev-ui-kit. Derived from the panel-lightness scalar exactly as the exhibits derive theirs by calc(), rather than tabulated as literals - one number retunes the panel, and HSL is implemented so the transcription keeps the derivation instead of flattening it to hex. Two semantic colour systems on one set of primitives: Role for parameters (five locked hues, canonical order, an absent role leaving a deliberate spectrum gap) and State for transport (idle/active/armed/recording/inert). Skin::lift modulates rather than replaces, per the state-precedence rule. Type sizes start at 15-16px with a test asserting nothing routinely read falls below the 14px legibility floor - band labels are the sanctioned exception, being scanned rather than read. 8 tests; one of them caught my own hand-computed HSL expectation being wrong, not the code."
    },
    {
     "ts": "2026-07-20",
     "text": "Step 3 (widget foundation) done. DELIBERATE DEPARTURE FROM ui-01 §8, recorded rather than slipped in: a widget is a VALUE, not a trait object. The proposal sketched a Widget trait; R-622 (panels generated from device profiles), R-1310 (panels assembled by scripts) and the layout designer all need a widget to be something describable, not merely callable - a tree of data can be generated, serialized and edited, a tree of closures can be none of those. The roster is a closed enum on purpose: this is our kit, not an external toolkit. Design and interaction are separated: the tree holds what a widget IS, hover/press live beside it keyed by id, and a test asserts the design is byte-identical before and after a hover, because a layout designer must never see interaction state. Every widget's label is a required field, so a control that cannot name itself cannot be constructed. Anchored absolute layout, no solver. Release off the pressed widget cancels. Dirty marks only what changed, and setting an unchanged value marks nothing. 18 tests plus two golden masters (kit_1x, kit_125x) rendering a described Control Bar through the skin and paint list with no imperative drawing in between - the property the designer story rests on."
    },
    {
     "ts": "2026-07-20",
     "text": "Anchor bug found by its own test: children were resolving against the parent's RESOLVED size rather than its DESIGNED size, so the growth term was always zero and nothing ever stretched. Fixed by threading the designed size through placement."
    },
    {
     "ts": "2026-07-20",
     "text": "Step 4, first pass: the tri-state Record button, the multi-field Counter, and the locator bank. Record has THREE intrinsic states (Off/Armed/Recording), not two - and it reports the state it was pressed IN rather than deciding what happens next, because whether a press arms, disarms or stops is a transport question and the transport is not in the kit (R-901). Armed blinks at 2 Hz off a phase the KIT owns, not the widget, so every armed control in the application pulses together; the phase is derived from the UI clock and never stored in the design, since a flashing widget must not serialize as 'currently bright'. Kit::animate returns whether a frame is owed and marks only the armed widgets. Counter fields are addressed individually: a press picks the field under the pointer, a drag moves that field alone at one step per 4 logical pixels, values are zero-padded to fixed width and clamped rather than wrapped. Which field is addressed is INTERACTION state, so it lives beside the design, not in it. Locators read at a glance - grey until filled - and an empty one asks to be stored while a full one recalls. STILL TO COME in step 4: the pop-up (needs an overlay pass for z-order) and the shuttle; typing into a counter field needs the text-input channel wired and is deliberately not faked."
    },
    {
     "ts": "2026-07-20",
     "text": "Two process notes from this pass. (1) A scripted edit to the golden scene silently no-op'd on a mismatched anchor, so the new widgets were never added - and the reference still differed, because the panel had grown. The byte count said 'changed'; only LOOKING at the image showed it had changed for the wrong reason. That is the argument for the comparator writing a .actual.png rather than reporting a diff size. (2) Locator numbers were drawn at the band size, 12px, below our own legibility floor - band labels are the sanctioned exception because they are scanned, and a locator number is read. Moved to the label size before blessing."
    },
    {
     "ts": "2026-07-20",
     "text": "The harness became a real client of the kit (Control Bar described as data; hit/event/paint/a11y all delegate), which needed one mechanism addition: nothing woke the event loop, so a flashing control could not flash. Mech::wake_after plus Host::tick, called once per loop iteration - and the application requests a wake only WHILE something is animating, so a still transport lets the loop sleep. Three defects surfaced immediately by operating it, which no test had asked about. (1) A cancelled press reported Intent::Released, so dragging off a button and letting go still fired it; now Intent::Cancelled, and the existing test was strengthened rather than left asserting the bug. (2) Play was a momentary button, so 'am I playing?' was invisible - and Record's third state depends on the answer. A state you cannot see is not a state: Play latches now, arming then starting playback becomes Recording, and Stop clears all of it. (3) Locators could be set but never cleared, which reads as stuck; shift-click clears, and the app reads the modifier off the event because the kit reports what happened, not what it meant. Also: the stand-in transport now advances the counter at 120 bpm while playing, which exercises set_field, dirty marking and the wake path together."
    },
    {
     "ts": "2026-07-20",
     "text": "Step 4 census complete: pop-up and shuttle join record, counter and locators. The pop-up needed the one genuinely new mechanism in the kit - an OVERLAY PASS. An open list must paint above widgets drawn after its owner and must receive clicks that land over them, which is z-order rather than document order; a test places a button below the pop-up precisely because it would win under document order. One overlay pass, because one thing floats: a general z-order system for a single case would be machinery in search of a problem. Dismissing a menu chooses nothing and, importantly, does not also operate whatever the dismissing click landed on. The shuttle is sprung: it tracks the pointer across its own width, clamps at its stops, and on release returns to rest AND reports it, because otherwise nothing downstream learns that scrubbing has stopped. Both answer the a11y contract - a pop-up reports its choice, not its options. 165 tests."
    },
    {
     "ts": "2026-07-20",
     "text": "TRIPWIRE: src/rust/ui_kit/src/kit.rs is 1181 lines, past the ~1000-line refactor-candidate threshold in the coding standard. Flagged, not split - the standard says never silently. The natural seam is drawing: the per-kind appearance code is roughly 350 lines and depends on nothing but the skin, the painter and the widget, so kit/draw.rs would leave tree, layout, hit testing and events in kit.rs. Awaiting the user's call."
    },
    {
     "ts": "2026-07-20",
     "text": "Refactor done at the tripwire rather than deferred: kit.rs 1181 -> 821 lines, with appearance moved to kit/draw.rs (380). The seam is real rather than arbitrary - drawing depends on nothing but the skin, the painter and the widget, while the tree, layout, hit testing and events stay together. A child module, so it still reaches Kit's private interaction state, which it needs precisely because hover and press modulate appearance WITHOUT belonging to the design. The golden masters passed unchanged after the move, which is the check that matters: a refactor that alters a pixel is not a refactor."
    },
    {
     "ts": "2026-07-20",
     "text": "Step 4 complete: typing into a counter field, which is focus plumbing rather than a widget and the first real use of the focus API. The two keyboard channels finally earn their split - COMPOSED TEXT supplies digits (so an input method works), RAW KEYS supply commit/cancel/correct/move (Enter, Escape, Backspace, Tab). Nothing is reported while typing, because a half-typed number is not a value anyone downstream should act on; the buffer is separate from the value, so Escape restores nothing - it drops the buffer, and the value was never touched. Out-of-range input CLAMPS rather than being refused, since a rejected keystroke leaves the user guessing which one it was. Dragging abandons a pending edit: you are setting the value now, not spelling it. The kit never touches the mechanism - Kit::editing() reports what wants focus and the application calls set_focus, so focus moves because a widget asked, the only way it may move (R-907). The app also gives an editing field the keyboard outright, or its own '0' shortcut would reset the interface scale mid-edit. 172 tests; kit.rs 969 lines after the split, back under the tripwire."
    },
    {
     "ts": "2026-07-20",
     "text": "Third instance of the same bug class, caught by the user operating the thing: the counter WAS there and clicking it DID work, but the addressed field was marked with a ground barely distinguishable from the slot it sat on, so the click read as doing nothing. Fixed with the exhibits' own idiom, which already solved this - an editable window under the pointer gets an AMBER OUTLINE, not a swapped ground, keeping amber digits on near-black as the most legible thing in the panel. Also added a console line so the terminal agrees with the screen. Two diagnostics kept from this: a render test in rev-app that draws the Control Bar exactly as shipped (into target/, ignored - a look, not an artifact), and a counter_states golden showing idle / addressed / typing side by side, because whether feedback is visible is a question only a still image answers. The pattern is now unmistakable: every bug this session that automated tests missed was 'the state exists and you cannot see it'."
    },
    {
     "ts": "2026-07-20",
     "text": "Step 5 done: the exhibits' settled interaction, transcribed into a `feel` module rather than scattered as literals - the constants ARE the control skin's feel, and changing one is a design decision, not a tuning tweak. Sliders are absolute (the cap jumps to the pointer and tracks); detents snap within 3.5% while dragging but only 1.5% while nudging, so a wheel notch can step PAST a detent instead of being swallowed by it - a neutral position that traps the wheel is worse than no detent. Wheel is coarse, TILT is fine: the horizontal wheel is not a second scroll axis on a control, it is the fine adjustment. Double-click returns to the detent, with the two-press recognition done in the kit because the mechanism reports presses and deciding that two are one gesture is a widget-level judgement. A counter has no fractional range, so a notch steps its addressed field by one. Kind::Slider added because the rules needed a subject; Kind::Knob deliberately NOT added, since it needs radial gradients and annular arcs that still have no caller - the slider is drawable with what we have, the knob is not, and that is the honest line."
    },
    {
     "ts": "2026-07-20",
     "text": "kit.rs is back to 1137 lines, over the tripwire again after the slider and the interaction rules. Flagging rather than splitting: the next seam is events (the whole event/nudge/slide/commit/typing machine is ~450 lines and depends on the tree only through find/mark), which would leave kit.rs as the tree, layout and queries. Recommend kit/event.rs on the same pattern as kit/draw.rs, at the user's word."
    },
    {
     "ts": "2026-07-20",
     "text": "Coding standard changed at the user's direction: keep files under ~1000 lines and SPLIT rather than writing one and asking. Splitting for size is routine work, not a checkpoint, and needs no permission - the seam is usually obvious and a child module keeps privacy intact. A file that genuinely cannot be split says so in its header; that is the stated exception. ('Nobody has ever complained of too many 500-line files.') Applied immediately: kit.rs 1137 -> 631, with kit/event.rs (520) taking the input machine, joining kit/draw.rs (464). The a11y queries were swept along by the extraction and moved back, because they are queries over the tree, not events. 183 tests and all three golden masters unchanged - the split moved no pixels and no behaviour."
    },
    {
     "ts": "2026-07-20",
     "text": "The app showed no wheel because it had nothing to wheel: the Control Bar tree had no continuous control and no pop-up. Added the pop-up, the shuttle and two sliders (one with a centred detent, one without) so every gesture step 5 settled is reachable - wheel coarse, tilt fine, detent snap, double-click home - and each reports to the console. Note that the wheel already worked on the counter's addressed field; it was reachable but undiscoverable, which is the same lesson a third time."
    },
    {
     "ts": "2026-07-20",
     "text": "kit/test.rs is 924 lines and rising, inside the rule but not by much. Next growth should split it along the same lines as the source: tree/layout, input, and the widget census each have their own fixtures already."
    },
    {
     "ts": "2026-07-20",
     "text": "Menu drag-through, from the user operating it: an open list now tracks the pointer and highlights provisionally, whether or not a button is held, so both gestures work - press-drag-release, and click-then-click. Releasing over an item chooses it; releasing anywhere else leaves the menu open, which is what makes a plain click-to-open work. Drawing rule settled while looking at it: EXACTLY ONE highlight bar. The first attempt gave the chosen item a blue ground and the hovered item a lifted blue ground - two blue rows asking the reader to work out which meant what. Now the bar follows the pointer and the current choice steps back to an amber label, because while a menu is open the question is 'what am I about to pick', not 'what did I pick last time'. A menu_open golden master records it, since that judgement is only answerable from a still."
    },
    {
     "ts": "2026-07-20",
     "text": "Wheel affordance, from the user operating it - one real bug and one missing signal. THE BUG: the wheel acted on the counter field a click last ADDRESSED, not the one under the pointer, so hovering the beats and wheeling changed the bars. The wheel event carries a position and I was ignoring it; it now aims where the pointer is. A stale test was passing a meaningless (0,0) position and had to be given a real one - it had been asserting the old behaviour by accident. THE SIGNAL: nothing said a control was wheel-able or which part of it you were aiming at. Two additions. The hovered counter field is now marked faintly, so the wheel's target is visible before anyone spins it. And Kit::cursor() reports a shape from what is hovered - vertical arrow over sliders and counters, horizontal over the shuttle, hand over anything pressed, and DEFAULT over an inert control, because promising a gesture that will be refused is worse than saying nothing. The application asks the mechanism for it, same handshake as focus: the kit still never touches the mechanism."
    },
    {
     "ts": "2026-07-20",
     "text": "Process note: a scripted patch silently no-op'd for the second time this session, applying an edit to event.rs whose anchor lived in kit.rs. Both times the failure was invisible until the compiler or an image complained. Rule for the rest of this work: after a scripted edit, grep for the thing that was supposed to appear before moving on."
    },
    {
     "ts": "2026-07-20",
     "text": "Read-and-inventory pass over the Macintosh Human Interface Guidelines (1992, 410pp) -> doc/revision_hig_inventory.md, with chapter citations so every claim is checkable rather than recalled. Proposed standing: R-939, where our requirements are silent on interaction BEHAVIOUR the document is the default authority - not for appearance, not for menu-bar architecture, not for its document model. The inventory found that seven rules we implemented, broke or fixed this session are stated there verbatim, including the two we got from user bug reports. It also found TWO DEFECTS immediately: buttons stayed lit when the pointer dragged off them (ch. 7 says a pressed control tracks the pointer and unhighlights, so the cancel is visible before it happens), and attention state survived activating another control. Both fixed. Departures recorded deliberately: the Save/Revert document model (R-201 abolishes unsaved state), single-level undo (ours is journaled and unlimited), and its modality tolerance (R-905/R-906 are stricter). Constructs it cannot answer - second mouse button, wheel and tilt, touch, DPI scale, semantic a11y trees, multi-window without a menu bar - are listed as ours to decide and record."
    },
    {
     "ts": "2026-07-20",
     "text": "R-939 and R-940 applied. R-939 makes AHIG 1992 the default authority for interaction behaviour where our requirements are silent, with an explicit precedence chain (requirements, then coding standard and inventories, then that document, then invention - and an invention is recorded with its reason). R-940 names the standing departures so nobody re-litigates them: no Save/Revert model, journaled unlimited undo, stricter modelessness. Cross-referenced from the coding standard (a new Interaction section, carrying the never-misreport-itself rule), from revision_poc.md as the third design source alongside Vision-for-function and the exhibits-for-appearance, and from CLAUDE.md so a new agent inherits it in the ground rules."
    },
    {
     "ts": "2026-07-20",
     "text": "Step 6, assembly: the Control Bar laid out as a strip rather than a test bench. Transport cluster, a divider, the mode toggles, a divider, the record-mode pop-up on the top row; counter, tempo, shuttle and eight locators below. Kind::Rule added for the dividers - it reports nothing and takes no input, and exists so clusters read as clusters without boxing each one. The counter gained a SEPARATOR: '|' for bar|beat|unit, '.' for a decimal tempo, ':' for SMPTE later - one widget serving all three rather than three widgets differing by a punctuation mark. The Control Bar is a window for now; whether it becomes a dockable pane is deferred and costs nothing, because R-928 keeps views placement-agnostic."
    },
    {
     "ts": "2026-07-20",
     "text": "Closed by the user after running rev-app: 'it looks quite good'. The text-versus-icon label option raised at the same time is NOT part of ui-03 - it is a later idea, unfiled by decision. Wheel-sensitivity highlighting on number fields confirmed on target."
    }
   ],
   "created": "2026-07-19",
   "completed": "2026-07-20"
  },
  {
   "id": "ui-04",
   "title": "Control Bar slice wired to engine",
   "kind": "major",
   "track": "ui",
   "status": "planned",
   "depends_on": [
    "ui-03",
    "eng-07"
   ],
   "requirements": [
    "R-906",
    "R-907"
   ],
   "links": [],
   "plan": "Transport drives engine; counter follows engine clock and edits mid-playback; key equivalents; always-active behavior verified. Stage 4 exit.",
   "verify": "",
   "notes": [],
   "created": "2026-07-19",
   "completed": null
  },
  {
   "id": "rec-01",
   "title": "Capture path: record_batch to journal, arm/modes",
   "kind": "major",
   "track": "recording",
   "status": "planned",
   "depends_on": [
    "midi-03",
    "ui-04"
   ],
   "requirements": [
    "R-801",
    "R-802",
    "R-807",
    "R-808"
   ],
   "links": [],
   "plan": "Record-arm; replace and overdub; captured events journaled as record_batch commands landing as direct events on the track.",
   "verify": "",
   "notes": [],
   "created": "2026-07-19",
   "completed": null
  },
  {
   "id": "rec-02",
   "title": "Two tracks recorded and replayed + kill-mid-take",
   "kind": "major",
   "track": "recording",
   "status": "planned",
   "depends_on": [
    "rec-01"
   ],
   "requirements": [
    "R-808",
    "R-1504"
   ],
   "links": [],
   "plan": "Record two MIDI tracks from hardware, replay them; kill -9 mid-take loses nothing committed. PoC completion test.",
   "verify": "",
   "notes": [],
   "created": "2026-07-19",
   "completed": null
  },
  {
   "id": "rec-03",
   "title": "16-ET party trick",
   "kind": "minor",
   "track": "recording",
   "status": "planned",
   "depends_on": [
    "eng-07"
   ],
   "requirements": [
    "R-002",
    "R-418"
   ],
   "links": [],
   "plan": "MHALL with a one-line tuning swap to 16-ET Mavila: audible proof the plumbing is degree-native.",
   "verify": "",
   "notes": [],
   "created": "2026-07-19",
   "completed": null
  },
  {
   "id": "dsp-01",
   "title": "Padlington JS inventory (read-only)",
   "kind": "minor",
   "track": "dsp",
   "status": "complete",
   "depends_on": [],
   "requirements": [
    "R-705"
   ],
   "links": [
    {
     "doc": "revision_poc.md",
     "anchor": "Padlington port plan"
    }
   ],
   "plan": "Read-only visit to Notorolla: patch fields, voice graph topology, envelope timings, spectral-profile construction. No Notorolla changes.",
   "verify": "Inventory written to doc/revision_padlington_inventory.md from padsynth.js, audio.js (voice + filter env + levels), instrument.js (defaults/params), notch/padsynth.mjs + meter-pad.mjs. Covers: bake pipeline formulas, bake-vs-playtime param partition + cache key, voice graph (eng-02 node census), AudioParam vocabulary (= eng-04 scope), level constants, test assets, and port decisions (spectra-not-bit-identity comparison, seed head offsets, formant table unification, FREF tuning-awareness). Zero Notorolla changes.",
   "notes": [],
   "created": "2026-07-19",
   "completed": "2026-07-19"
  },
  {
   "id": "dsp-02",
   "title": "Spectral builder (seeded) + spectra tests",
   "kind": "major",
   "track": "dsp",
   "status": "complete",
   "depends_on": [
    "dsp-01",
    "boot-03"
   ],
   "requirements": [
    "R-705",
    "R-706",
    "R-713",
    "R-720"
   ],
   "links": [
    {
     "doc": "completed/revision_dsp02_proposal.md",
     "anchor": "dsp-02 proposal"
    }
   ],
   "plan": "PADsynth table construction in Rust: profile morphs, formants, Gaussian spread, seeded phases, IFFT. Magnitude-spectrum comparison against JS-built tables (phase-independent).",
   "verify": "rev-dsp built: profile/spectrum/bake/rand, 27 unit tests as analytic identities ported from notch/padsynth.mjs (saw 1/k, pulse-at-0 odd-only square, saw-at-1 odd-only triangle, tilt 1/k^e, formant bypass/peak/size/universality, stretch placement, energy-constant bandwidth, air matched at its ends, RMS normalization, bake determinism, nothing above the band limit). Half-octave table set (16 tables, 8 MB) with the band limit at Nyquist/r_max = 20.2 kHz, so fold-back is zero by construction. rev-engine gained a per-note TableId override chosen by TableSet::nearest. rev-testkit gained spectrum (Blackman-Harris, fundamental_hz, centroid_near, worst_inharmonic). Aliasing sweep + pitch assertion across 37 notes, 55-3520 Hz. MHALL now plays the real Padlington with Notorolla's Harpington envelope. 341 workspace tests green, clippy clean.",
   "notes": [
    {
     "ts": "2026-07-21",
     "text": "FINDING: with the band limit in place the residual inharmonic energy is NOT fold-back and NOT the interpolator - it is the resampling images of a looped table, at (r-1)*SR +/- k*f0. Identified by arithmetic: at 1568 Hz read at 1.0595 the worst components measured at 9690, 11256 and 12823 Hz, which (0.0595*48000) - k*1568 reproduces exactly. Swapping linear for Catmull-Rom interpolation moved them 2 dB. Worst across the keyboard is -27.6 dB at 2489 Hz. The reference instrument has them too and worse (octave spacing reads up to 1.414). Removing them means reading oversampled - the per-node oversampling conversation dsp-02 sec 4.4 deferred. FLOOR_DB is a regression guard, not a certificate of inaudibility."
    },
    {
     "ts": "2026-07-21",
     "text": "CORRECTION to dsp-02 sec 4.2: 'linear interpolation is load-bearing, do not upgrade it' is true only in the source's geometry, where content crosses Nyquist and linear's droop attenuates it. Under the strict band limit nothing crosses Nyquist, so a better interpolator would be weakly better, not worse. Measured, not argued: Catmull-Rom gained 2 dB."
    },
    {
     "ts": "2026-07-21",
     "text": "The measurement was wrong three times before it was right, each time in the flattering direction: a cents-only tolerance narrower than the window's main lobe measured a pure harmonic series as 6 dB dirty; peak-picking a PADsynth partial (a noise band with random per-bin phase) reported notes 16-32 cents flat; and at 2^15 the one-bin smear floor quantized low-note pitch by 23 cents. Every one looked like an instrument defect and was a measurement defect."
    },
    {
     "ts": "2026-07-21",
     "text": "Harpington arrived in two goes. The project export could not contain it: storage.js keeps the patch catalog under 'notorolla.patches', user-global and deliberately outside every project file, so the exported lanes carried only a factory Vesperia Init. The real preset came from localStorage: kind padlington, saw, harmonics 37.79 (rounds to 38), bandwidth 19.83 cents, bwScale 0.706. That bwScale is the character - under 1, so the smear grows slower than constant-cents and the upper partials stay narrow instead of merging into a continuum, which is why it reads as a struck string with a defined top. MHALL, the pitch sweep and the sched integration tests all play it now. Its longer release (0.44 s against the stand-in's 0.12) pushed SPAN in sound.rs from 18 s to 20 s: the last note was still correctly ringing at 17 s, where the old assertion looked."
    }
   ],
   "created": "2026-07-19",
   "completed": "2026-07-21"
  },
  {
   "id": "dsp-03",
   "title": "Padlington voice certified",
   "kind": "major",
   "track": "dsp",
   "status": "planned",
   "depends_on": [
    "dsp-02",
    "eng-05"
   ],
   "requirements": [
    "R-705",
    "R-711",
    "R-713"
   ],
   "links": [],
   "plan": "Assembled voice; offline render metered against reference (RMS/peak/centroid); real note_on/note_off lifecycle (fire-and-forget retires); MHALL switches from sine to Padlington.",
   "verify": "",
   "notes": [],
   "created": "2026-07-19",
   "completed": null
  },
  {
   "id": "misc-02",
   "title": "Generated schema browser + file-map checker",
   "kind": "minor",
   "track": "misc",
   "status": "complete",
   "depends_on": [
    "core-02"
   ],
   "requirements": [],
   "links": [
    {
     "doc": "completed/revision_misc02_proposal.md",
     "anchor": ""
    }
   ],
   "plan": "Move the DDL to schema.sql (include_str!) with an annotation convention; xtask schema generates doc/revision_schema.json from live introspection plus comment extraction, rendered by revision_schema.html grouped by architecture with FK links and mermaid ER diagrams; xtask filemap implements the deferred two-way checker; both wired into CI's lint job. Six decisions in the proposal's §6.",
   "verify": "All six decisions approved and implemented. DDL moved to src/rust/store/src/schema.sql via include_str! and fully annotated (every column); cargo xtask schema generates doc/revision_schema.json - 6 architecture groups, 14 objects, structure from PRAGMA introspection of a live probe database, prose from the SQL annotations - with --check mode for CI; doc/revision_schema.html renders it grouped, with FK links and per-group mermaid ER diagrams; cargo xtask filemap enforces the map both ways (18 entries, meta.ignore for deliberate gaps). Both wired into CI's lint job. Verified locally: schema generates and --check reports current, filemap passes, 75 workspace tests still green, fmt and clippy -D warnings clean. Enforcement proved itself on first run by rejecting ten unannotated columns. HTML viewer eyeballing is the user's (serve the repo and open doc/revision_schema.html).",
   "notes": [],
   "created": "2026-07-20",
   "completed": "2026-07-20"
  },
  {
   "id": "misc-01",
   "title": "Coding standard document",
   "kind": "minor",
   "track": "misc",
   "status": "complete",
   "depends_on": [],
   "requirements": [],
   "links": [
    {
     "doc": "revision_coding_standard.md",
     "anchor": ""
    }
   ],
   "plan": "Extract coding standards into doc/revision_coding_standard.md: pithy, deviations-and-decisions-only over a default of ordinary Rust convention (naming, layout, file size, comments, test placement). Trim getstarted Conventions to a pointer; add to read-first list and meta.docs.",
   "verify": "Draft reviewed in discussion and approved by user (make it so, 2026-07-19); doc created; getstarted Conventions section replaced by pointer; meta.docs updated.",
   "notes": [],
   "created": "2026-07-19",
   "completed": "2026-07-19"
  },
  {
   "id": "misc-03",
   "title": "License record + third-party attribution (xtask license)",
   "kind": "minor",
   "track": "misc",
   "status": "planned",
   "depends_on": [
    "ui-01"
   ],
   "requirements": [
    "R-1514"
   ],
   "links": [
    {
     "doc": "revision_coding_standard.md",
     "anchor": "Dependencies & licensing"
    }
   ],
   "plan": "cargo xtask license: walk the resolved dependency tree, emit doc/revision_license.json (every dependency, its SPDX expression, the arm adopted, a flag on any expression containing a copyleft arm) plus a rendered attribution page; --check mode in CI's lint job, matching the schema/filemap pattern. Records permissive-over-copyleft choices explicitly (self_cell: Apache-2.0 adopted, GPL-2.0-only declined) rather than leaving them inferred. Distinguishes SPDX OR (a choice) from AND (both apply). Satisfies R-1514, whose artifact ships with distributed builds.",
   "verify": "",
   "notes": [
    {
     "ts": "2026-07-20",
     "text": "Placeholder created 2026-07-20 with the requirement block. Waits on ui-01 because the four UI dependencies are what make the record non-trivial (Unicode-3.0, Zlib, BSD-2/3-Clause, 0BSD, and self_cell's Apache-or-GPL dual all arrive with them)."
    }
   ],
   "created": "2026-07-20",
   "completed": null
  },
  {
   "id": "ui-05",
   "title": "Observation viewer — the scrolling log window",
   "kind": "major",
   "track": "ui",
   "status": "planned",
   "depends_on": [
    "eng-03",
    "ui-07"
   ],
   "requirements": [
    "R-1510"
   ],
   "links": [
    {
     "doc": "completed/revision_eng01_proposal.md",
     "anchor": "9.7"
    }
   ],
   "plan": "A live tail of rev-log: auto-scrolling list, filter by creator and severity, drained per UI frame. Scheduled after eng-03 by decision 11 of eng-01, because the user watches logs and there is now volume worth reading. Reads the same forever file the writer thread owns, over a read-only connection (WAL means the reader never blocks the writer).",
   "verify": "",
   "notes": [
    {
     "ts": "2026-07-21",
     "text": "Design note from eng-01 §9.6: Trace is a firehose (per-block records at ~100 blocks/second), so the viewer needs per-creator filtering, and the default threshold stays Info. The shuttle already logs at Trace for exactly this reason."
    }
   ],
   "created": "2026-07-21",
   "completed": null
  },
  {
   "id": "ui-06",
   "title": "Read-only piano roll — MHALL visible, playing",
   "kind": "major",
   "track": "ui",
   "status": "planned",
   "depends_on": [
    "core-03",
    "eng-07",
    "ui-07"
   ],
   "requirements": [
    "R-941",
    "R-942",
    "R-943",
    "R-312"
   ],
   "links": [],
   "plan": "Resolve v_realized rows to frequencies through their tuning, map log-f to y, draw note rectangles, a degree ladder from the applicable tuning, and a playhead driven by the engine's position snapshot. READ-ONLY: no snapping, no drag, no row model, no keyboard widget - editing is a later item and is the thing that raises 'which tuning does a dragged note belong to'. Small precisely because R-941 removes the row machinery rather than adding to it.",
   "verify": "",
   "notes": [
    {
     "ts": "2026-07-21",
     "text": "Blocked on a rev-ui-kit checkpoint: the kit has absolute/anchored layout with fixed-size widgets and NO scrolling and NO zoom. A roll needs both, and that is a public API addition (ground rule 2). Flagged before implementation rather than discovered during it."
    }
   ],
   "created": "2026-07-21",
   "completed": null
  },
  {
   "id": "misc-04",
   "title": "Plan viewer: one `now`, a work log, and a checker",
   "kind": "minor",
   "track": "misc",
   "status": "complete",
   "depends_on": [],
   "requirements": [],
   "links": [],
   "plan": "The `now` banner had grown to four narrative entries and ~4900 characters above the plan itself. Split state from record: `now` holds exactly one short entry, retired entries move to the archive's `log` with timestamps intact, and the viewer renders the log collapsed at the foot of the page.",
   "verify": "revision_plan.json `now` is one entry (197 chars); revision_plan_archive.json gained `log` with the four retired entries, newest-first, nothing edited. Viewer reads archive.log into a collapsed <details>. New `cargo xtask plan` validates the shape the viewer reads - {ts,text} entries everywhere, the 600-char `now` ceiling, known status/kind/track vocabulary, depends_on resolving against plan+archive, complete<->completed agreement - and runs in CI's lint job beside filemap. Six xtask tests, one of which checks the real file.",
   "notes": [
    {
     "ts": "2026-07-21",
     "text": "Written because I got the shape wrong twice in one session: a `now` entry keyed `at` instead of `ts` rendered as the literal string 'undefined' at the top of the document, and four item notes written as bare strings would have rendered '[undefined] undefined' in every tooltip. Neither was caught by anything - the viewer has no validation and the plan had no checker. The failure mode is quiet and the cost is high, because a memory nobody trusts gets re-derived from scratch."
    }
   ],
   "created": "2026-07-21",
   "completed": "2026-07-21"
  },
  {
   "id": "ui-07",
   "title": "[checkpoint] Scrollable pane, scroll bars, zoom",
   "kind": "between",
   "track": "ui",
   "status": "complete",
   "depends_on": [
    "ui-03"
   ],
   "requirements": [
    "R-938",
    "R-939",
    "R-943",
    "R-1510"
   ],
   "links": [
    {
     "doc": "completed/revision_uikit_scroll_proposal.md",
     "anchor": "ui-kit scroll proposal"
    }
   ],
   "plan": "The surface ui-05 and ui-06 are both built on: Kind::Pane with the kit owning frame, bars, clip, offset and scale, and the application painting the interior. Scroll bars never hidden and always reserved; proportional thumb; live drag; no arrows. Zoom is ours - the 1992 book has none - with independent axes, log-space vertical, and pointer- or playhead-anchoring.",
   "verify": "rev-ui-kit gained pane.rs (geometry, 17 unit tests) and Kind::Pane with Intent::Scrolled/Zoomed. Kit::paint_with hands a PaneArtist the interior inside a kit-owned clip - the first and only place the kit lets an application paint. Bars reserved unconditionally, proportional thumb with a floor and correct travel, gray-area paging with the book's one-unit overlap, thumb drag with snap-back and resume as ordinary offset updates. Zoom cluster is Vision's: Lucide zoom-in/zoom-out glyphs, logarithmic slider, 2^(1/8) steps, pointer-anchored wheel zoom under ctrl. Four golden screenshots at 1x and 1.25x plus five behaviour tests; rev-pane demo bin with a labelled grid over a million-unit extent. 397 workspace tests green, clippy clean, file map current.",
   "notes": [
    {
     "ts": "2026-07-21",
     "text": "HIG ch. 5 'Windows' pp. 158-167 read from the source rather than recalled, and recorded as inventory section 3a. It answers more than expected: bars are inactive-not-hidden in the book itself, so reserving the gutter is faithful rather than a departure. Three knowing departures recorded in inventory section 7 - proportional thumb (the 1992 box carries position only), live drag (the book redraws on release), and no arrows (unused furniture on hardware with a wheel; gray-area paging and its one-unit overlap rule survive)."
    },
    {
     "ts": "2026-07-21",
     "text": "Two corrections after review. (1) The snap-back on dragging out of a scroll bar is NOT a cancel - Intent::Cancelled fires at release, never on leaving, so the kit already behaves the way the book and current applications do. Modelled as ordinary offset updates: the snap-back is an update back to the drag's origin, returning resumes, release-outside emits nothing because the value is already there. Nothing to revert. (2) Escape does not apply to navigation at all, stated as a general rule - Escape cancels an uncommitted change to the document, and scrolling and zooming are not that. Zoom gained a visible continuous control per axis, at the end of that axis's scroll bar, which is exactly the one addition HIG p.161 permits there; logarithmic slider position so equal travel is equal ratio; 2^(1/8) steps; zoom-to-fit and zoom-to-selection reserved."
    },
    {
     "ts": "2026-07-21",
     "text": "The zoom control is Vision's, exactly: [-] and [+] magnifier buttons at the far end of the bar with the slider between them when it fits, buttons flush when it does not. Better than the two-tier swap it replaces because degradation is subtractive - nothing is substituted, the middle is simply absent, and the buttons never move. It also removes the second-scroll-bar confusion by construction rather than asking the skin to paint around it. The vertical cluster is the horizontal one rotated 90 degrees clockwise, minus above plus, which departs from ch.7 p.214's up-means-more; taken because p.214 guards against unclear direction and the magnifier glyphs make direction explicit. The cluster spends the whole scroll-bar-region budget p.161 allows, so nothing else ever goes there - no split bar, no page indicator, no status strip."
    },
    {
     "ts": "2026-07-21",
     "text": "Two decisions were approved but deliberately NOT built, both recorded in proposal section 11. Press-repeat (holding the gray area or a magnifier) is absent: repeating needs a tick that can emit an Intent and the kit's animate() returns a bool, so it needs an event channel this checkpoint did not approve. And section 5.3's live-drag fallback is absent because it was specified against a paint-cost measurement the pane does not yet take - building it first would have meant guessing the threshold, which is what 5.3 existed to avoid."
    },
    {
     "ts": "2026-07-21",
     "text": "Two defects found by looking rather than by testing, which is the whole argument for decision 19. The zoom slider first took whatever gutter room was left after the buttons, leaving the scroll track exactly zero pixels long at ordinary sizes - the gutter belongs to the scroll bar and the cluster is a guest with a fixed length. And the magnifier glyph, invented, drew the lens as four straight strokes and read as a box with a dash in it; section 6.4 leans on that glyph to justify the vertical cluster reading downward, so a glyph that promises nothing undermines the departure. It now uses Lucide's zoom-in/zoom-out geometry, transcribed not bundled, ISC, recorded in asset/asset.json."
    },
    {
     "ts": "2026-07-21",
     "text": "Amended after operating the demo (proposal section 12). Bar width moved to Skin::metric::scroll_bar and went 15 -> 22 logical px, thumb floor to bar*1.5; size-adaptive bars considered and rejected because a target that changes size between windows defeats the muscle memory the fixed cluster buttons were designed for. The wheel now takes its axis from whatever it is over - cluster zooms that axis with no modifier, bar scrolls that axis, interior scrolls or zooms with the modifier at the pointer - which is the kit's existing 'the wheel aims where you are looking' rule reaching the pane. Sub-widget hover added so furniture highlights and content never does. The zoom modifier stays ctrl and is recorded as unsettled: Cubase and Resolve disagree."
    },
    {
     "ts": "2026-07-21",
     "text": "Second round of hand-tuning (proposal section 12 F and G). Drag tolerance doubled to bar*3 and the zoom step doubled to 2^(1/4); bar width kept at 22. And the thumb now lights whenever the pointer is anywhere in its track, not only on the thumb - amendment C made that necessary, because a rule about what the wheel is over creates an obligation to show what it is over."
    }
   ],
   "created": "2026-07-21",
   "completed": "2026-07-21"
  }
 ]
}