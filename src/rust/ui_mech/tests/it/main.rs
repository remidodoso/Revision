//! Integration tests for `rev-ui-mech`: one umbrella binary per crate, per the
//! coding standard.
//!
//! These are the screenshot golden masters — bit-identical comparison, affordable
//! because rendering is CPU-side with bundled fonts and a pinned locale.

mod golden;
